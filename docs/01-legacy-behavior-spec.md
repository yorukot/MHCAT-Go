# Legacy Behavior Spec

Status: Phase 1 consolidated. This is the Gate A behavior baseline. Exact command option JSON, embed payloads, and component payload grammar still need generated snapshots before implementation.

## Runtime Behavior

- Bot starts through `shard.js`, then each shard runs `index.js`.
- Mongo connects before handlers load and before Discord login.
- Global slash commands are created on every shard `ready`; orphan application commands are deleted.
- Slash dispatch is in `events/SlashCommands.js`.
- Prefix dispatcher exists for `commands/**/*.js` with prefix `/`, but no `commands/` directory exists in this checkout.
- Direct message-content restart commands exist in `index.js`.
- Declared command cooldowns are metadata only; central enforcement was not found.
- Permissions are checked ad hoc inside commands and components.
- Many interactions perform Mongo, Discord REST, member fetch, or rendering work before a consistent defer policy.

## Slash Command Matrix

Compact group matrix. Full command snapshot tests must preserve exact command names, option names, localizations, descriptions, and default permissions.

| Command group | Commands | Inputs | Permissions | Cooldown | Mongo reads/writes | Discord/external side effects | Response behavior | Legacy edge cases | Feature parity status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 群組防護 | `詐騙網址回報`, `帳號需創建時數`, `防詐騙網址` | URLs, hour count, channel/delete toggles | public or `KickMembers`/`ManageMessages` | mostly 10s metadata | `not_a_good_web`, `create_hours`, `good_web` | report webhook, join kick, message delete | defer/edit | user-controlled regex risk; cooldown not enforced | anti-scam toggle/report/deletion are parity-audited in [77-anti-scam.md](77-anti-scam.md); account-age config/member gate remain a separate onboarding slice |
| 抽獎系統 | `抽獎設置` | date, winner count, gift, content, roles, max entrants | `ManageMessages` metadata; runtime check unreachable because disabled response returns first | 10s metadata | disabled command writes nothing; existing-message buttons read/update `lotters.member`/`end` | reroll sends winner messages | ephemeral defer/edit with green unavailable embed; existing buttons defer/edit ephemerally | creation returns unavailable before legacy logic; existing rows can still accept entries/search/reroll/stop | parity-audited: disabled-command response and separately gated existing-message component runtime preserve valid legacy UI/data behavior; creation/panel generation remain disabled |
| 統計系統 | `統計系統查詢`, `統計系統創建`, `統計身分組人數`, `統計系統刪除` | stat type, channels, roles | mutating paths use `ManageMessages` | 10s/30s metadata | `Number`, `role_number` | create/rename statistic channels | reply/followUp/edit | old channel type strings under discord.js v14 | in-progress: static `/統計系統查詢`, channel-create `/統計系統創建`, role-count `/統計身分組人數`, config-row `/統計系統刪除`, and gated 20-minute rename worker implemented behind explicit flags |
| 自動通知 | `automatic-notification`, `自動通知列表`, `自動通知刪除` | cron/time menus, channel, message | `ManageMessages` | 10s metadata | `cron_set` | cron channel sends | modal/defer/edit/reply | cron validation fragile; jobs not centrally tracked | implemented behind separate disabled-by-default config/sync and delivery gates: setup supports direct cron and owner-scoped weekday/hour/minute selects; list/delete preserve legacy UI; the recurring worker uses one Mongo lease owner and `Asia/Taipei` schedule time |
| 代幣系統 | `簽到`, `簽到列表`, `代幣查詢`, `代幣排行榜`, `代幣增加`, `代幣重製`, `coin-related-settings`, `剪刀石頭布`, `代幣遊戲`, `代幣商店`, `my-profile` | user, amount, wager/options, shop quantity | mixed public, `ManageMessages`, owner-like paths | 1s-100s metadata | `coin`, `gift_change`, `sign_list`, `ghp`, `text_xp`, `voice_xp`, `work_user`, `work_set` | DMs, images, buttons, games | reply/defer/followUp/canvas/buttons | non-atomic coin/inventory updates; transactional game wagers | in-progress: `/代幣查詢` read-only, gated `/代幣排行榜`, gated `my-profile`, gated `/簽到`, gated read-only `/簽到列表`, gated `/coin-related-settings`, gated `/代幣增加`, gated `/剪刀石頭布`, gated `/代幣遊戲` with active timeout settlement, gated `/代幣重製`, and gated `/代幣商店`; game restart reconciliation remains open |
| 打工系統 | `打工系統` subcommands | setup/add/delete/interface/add energy | public interface, others `ManageMessages` | 5s metadata | `work_set`, `work_something`, `work_user`, `coin` | dashboard link, work buttons, minute payout job | defer/modal/buttons | scheduler-owned payout; energy/type drift | in-progress: crash-idempotent one-shot and recurring payout paths, gated `新增打工事項` dashboard redirect, `打工介面` list/captcha/detail/start UI, and setup/delete/energy admin writes; production payout ownership pending smoke/audit |
| 實用工具 | `ping`, `info`, `help`, `翻譯`, `自動聊天頻道`, `自動聊天頻道刪除` | language/text, channel | mostly public; chat setup `ManageMessages` | 0s-10s metadata | `chat`, `chatgpt`, `chatgpt_get` | Google Translate, select menu, bot/guild/user/shard info, local replies, external-worker handoff | reply/defer/followUp/select/message reply | translate needs timeout/defer; external paid worker deployment unknown | in-progress: utility read-only/info/help, translate, gated auto-chat config/delete, local fallback, and transactional paid `chatgpts` handoff implemented; external worker must be confirmed separately |
| 扭蛋系統 | `扭蛋`, `扭蛋獎池增加`, `扭蛋獎品編輯`, `扭蛋獎池刪除`, `扭蛋獎池查詢` | gift name/code/chance/count, draw count | draw/list public; edits `ManageMessages` | 1s/10s metadata | `gift`, `coin`, `gift_change` | DM prize codes, notification channel | defer/followUp/edit | prize count and coin races | in-progress: `/扭蛋`, read-only `/扭蛋獎池查詢`, one-row `/扭蛋獎池增加`, one-row `/扭蛋獎品編輯`, and one-row `/扭蛋獎池刪除` implemented behind explicit flags; shop is tracked under the economy shop slice |
| 私人頻道 | `私人頻道設置`, `私人頻道刪除` | ticket category/admin/everyone settings | `ManageMessages` | 10s metadata | `ticket` | create/delete ticket text channels via `tic`/`del` | modal/defer/edit | exclusive ownership, duplicate singleton audit, channel permissions, and compensated Discord/Mongo failures | parity-audited; exact UI, data, ownership, smoke, and rollback are in the [ticket parity contract](74-ticket.md) |
| 公告系統 | `公告發送`, `公告頻道設置` | announcement content, channel, tag/title/color | `ManageMessages` | 10s metadata | `guild`, `ann_all_set` | announcement channel send; config writes channel settings | modal/defer/edit/component confirm | Config/send/relay use separate gates and exclusive route ownership; tag mentions are suppressed and attachment-only messages are retained intentionally | parity-audited; exact UI, six-second confirmation, relay ordering, Mongo compatibility, ownership, smoke, and rollback are in the [announcement parity contract](76-announcement.md) |
| 警告系統 | `警告`, `警告紀錄`, `警告清除`, `警告全部清除`, `警告設定` | user, reason, warning index, threshold action | mostly `ManageMessages` | 10s metadata | `warndb`, `errors_set` | DM user, kick/ban threshold | defer/edit | one advertised permission lacks runtime check | in-progress: `/警告紀錄`, `/警告設定`, `/警告清除`, `/警告全部清除`, and `/警告` implemented behind separate gates |
| 生日系統 | `生日系統` subcommands | birthday date/time, message/channel/role settings | config/admin paths `ManageMessages` | 5s metadata | `birthday`, `birthday_set` | intended birthday messages/roles | defer/select/edit | sender/role scheduler commented out; legacy allow-admin path forgot to save its replacement row | in-progress: gated `祝福語設定`, `增加`, `是否允許管理員設定`, `刪除`, and `生日列表`; delivery scheduler not-started |
| 管理系統 | `刪除訊息`, `投票創建`, `選取身分組-表情符號`, `選取身分組刪除-表情符號`, `選取身分組-按鈕`, `set-log-channel`, `刪除資料`, `兌換`, `查看餘額` | message count, poll data, role/message/reaction, log toggles, delete target, code | mostly `ManageMessages` | 0s-60s metadata | `message_reaction`, `btn`, `poll`, `logging`, `code`, `chatgpt_get`, many config collections | bulk delete, role reactions/buttons, logging config, destructive config deletes | defer/modal/select/edit | delete-data destructive; poll member/export fetches and exclusive ownership | poll flow is parity-audited in the [poll parity contract](75-poll.md); role-selection and logging have separate contracts; other listed admin commands retain their own gated status |
| 加入設置 | `加入訊息設置`, `退出訊息設置`, `加入身份組設置`, `加入身份組刪除`, `驗證`, `驗證設置` | message templates, roles, channels, captcha input | setup `ManageMessages`; verify public | 5s/10s metadata | `join_message`, `leave_message`, `join_role`, `verification` | role add, nickname set, captcha | modal/defer/edit | `join_message.enable` unique Boolean risk; legacy verification embeds captcha answer in custom IDs | in-progress: join-role config, welcome-message config, welcome/leave event delivery, verification setup config, and gated `/驗證` captcha/role/nickname flow implemented behind explicit flags; account-age gate is covered under 群組防護/create_hours |
| 語音包廂 | `語音包廂設置`, `語音包廂刪除`, `上鎖頻道` | voice category/name/limit/lock/password | public command definitions; setup/delete require runtime `ManageMessages`; lock requires room ownership | 10s metadata | `voice_channel`, `voice_channel_id`, `lock_channel` | create/move/delete voice channels, permission overwrites, modal password | defer/edit | voice state ownership and cleanup are shard-sensitive; stage deletion follows the legacy category branch | parity-audited: config set/delete, dynamic room lifecycle, lock seed/password replacement, user-scoped prompt/disconnect/DM, and password modal unlock match the recorded contract in `docs/72-voice-room-config.md`; voice XP and voice logging remain separate gated event paths |
| 經驗系統 | `聊天經驗`, `語音經驗`, `聊天排行榜`, `語音排行榜`, settings/delete/role/reset/add commands | user/channel/background/color/message/role/xp | reads public; writes `ManageMessages`/`KickMembers`/owner-like paths | 10s metadata | `text_xp`, `voice_xp`, `text_xp_channel`, `voice_xp_channel`, `chat_role`, `voice_role` | role rewards, rank images | defer/reply/edit/canvas/buttons | XP/level stored as strings; rank scans guild docs | in-progress: disabled-response `聊天經驗`/`語音經驗`, read-only `聊天排行榜`/`語音排行榜`, text/voice announcement config, text XP accrual, the 30-second voice XP loop with startup reconciliation, text/voice reward roles and coin rewards, `經驗值改變`, and `經驗值重製` are implemented behind explicit gates; the unreachable legacy profile-card branches remain inactive |

### Implemented Low-risk Economy Slice

| Command name | Type | Category | Inputs | Permissions | Cooldown | Mongo reads | Mongo writes | Discord API calls | External API calls | Response type | Error behavior | Legacy edge cases | Feature parity status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `代幣查詢` | slash | 代幣系統 | optional user option `使用者` | public | metadata `10`; not centrally enforced in legacy | `coins` by `{guild, member}`, then `gift_changes` by `{guild}` only when balance exists | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred ephemeral edit; optional selected-user lookup | none | legacy-style ephemeral embed | no balance returns legacy red embed title | missing `gift_changes` uses default `500` and preserves footer quirk `<user>還差:500`; footer icon is invoker avatar | implemented behind `MHCAT_FEATURE_ECONOMY_QUERY_ENABLED` |
| `代幣增加` | slash | 代幣系統 | required user `使用者`, required string `增加或減少`, required integer `數量` | `ManageMessages` (`8192`) | metadata `10`; not centrally enforced in legacy | `coins` by `{guild, member}` | creates missing `coins` rows for add, updates duplicate `{guild,member}` rows for existing balances, legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | none | legacy-style public embed | reducing below zero returns the legacy red `不可減到負數!` embed; adding above `999999999` returns the legacy red max-balance embed | legacy created missing add rows with `today: true`; Go stores numeric `today: 1` for rollback-compatible BSON decoding and updates duplicate rows consistently | implemented behind `MHCAT_FEATURE_ECONOMY_COIN_ADMIN_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_COIN_ADMIN`; test only against disposable staging data |
| `剪刀石頭布` | slash | 代幣系統 | required integer `使用多少代幣來進行`; required string choice `剪刀石頭或布` | public | metadata `10`; not centrally enforced in legacy | `coins` by `{guild, member}` | updates existing duplicate `{guild,member}` `coins.coin` rows together; legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | none | legacy-style public result embed | missing balance returns `你沒有足夠的代幣進行此次遊玩!`; insufficient balance returns same text without `!`; invalid wager preserves legacy max/min messages | tie subtracts `parseInt(wager / 2)` equivalent; losing subtracts full wager; winning adds full wager and intentionally does not cap the resulting balance at `999999999` | implemented behind `MHCAT_FEATURE_ECONOMY_RPS_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_RPS`; test only against disposable staging data |
| `代幣遊戲` | slash + legacy buttons | 代幣系統 | subcommands `21點`, `知識王`, `比大小`; each requires user `跟誰玩` and integer `賭注` | public participants | metadata `10`; not centrally enforced in legacy | `coins` by `{guild, member}` plus bundled legacy `topic.json` knowledge bank | accepts game invite, transactionally reserves both wagers, transactionally settles the pot back to both players according to game result or forfeit; updates duplicate `{guild,member}` rows together; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public follow-up for invite; component updates/replies; background transition/timeout edits | none | legacy-style invite/acceptance/start/reveal/draw/result/timeout UI and legacy button IDs | missing/insufficient challenger or opponent balance returns legacy-style errors; invitations reject acceptance at the legacy 30-second deadline; stale/cross-user components return safe ephemeral errors; delayed or timeout settlement errors fail closed and are logged | Go intentionally validates the invited user before debiting, fixing the legacy bug where the challenger could click accept and debit both players before receiving the not-invitee error; Mongo transactions intentionally prevent one-sided reserve/settle writes and require a replica set or sharded cluster; phase/generation callbacks preserve the 500-millisecond knowledge start, five-second knowledge reveal and higher/lower draw, knowledge's carried countdown and strict 21st tick, and blackjack's strict 31st tick; serialized claims prevent component/transition/timer double settlement; active process-local state remains lost on restart | implemented behind `MHCAT_FEATURE_ECONOMY_GAME_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_GAME`; test only against disposable staging `coins` data; see `docs/67-economy-game-lifecycle.md` |
| `代幣商店` | slash + legacy buttons | 代幣系統 | subcommands `商品增加`, `商品刪除`, `商品查詢`; add requires name, price, description, auto-delete, optional code, role, count; delete requires `商品id` | add/delete `ManageMessages` (`8192`); query/purchase public | metadata `10`; runtime checks mutating subcommands | `ghps` by `{guild, commodity_id}`, `coins` by `{guild, member}` | creates/deletes `ghps` rows; purchase subtracts `coins.coin` and decrements or deletes auto-delete inventory rows; legacy global slash dispatcher increments `all_use_count`, Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit for slash; component message updates and ephemeral quantity errors | optional existing-role add and direct-message prize code | legacy-style shop/list/detail/quantity/success embeds and raw numeric/`ghp` custom IDs | overlong names, invalid prices/counts, overfull shop, missing items, insufficient coins, unassignable roles, too-large quantities, and quantity >1 for an existing role item return legacy-style errors | preserves legacy field names, free-text whitespace, UTF-16 name limits, explicit-zero count fallback, requester-scoped 10-minute collectors, button grammar, empty backspace display, missing-role multi-purchase behavior, and zero-count rows when a multi-stock purchase exhausts inventory; checked cost arithmetic prevents wrapped balance credits while retaining the legacy insufficient-coins response | implemented behind `MHCAT_FEATURE_ECONOMY_SHOP_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_SHOP`; test only against disposable staging `ghps`/`coins` data |
| `代幣重製` | slash + message confirmation | 代幣系統 | optional integer `除以多少` | guild owner only | metadata `100`; runtime checks guild owner ID | `coins` by `{guild}` | omitting `除以多少` or passing `0` deletes all guild `coins` rows; nonzero divisor updates every guild balance to `Math.round(coin / divisor)`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | immediate public warning reply; confirmation result sends a channel message | message-create event with Message Content intent for confirmation | warning content matches legacy; success embed title says `成功重製伺服器內所有代幣` | non-owner, wrong confirmation, and missing guild data return legacy-style red embeds | preserves the one-shot same-channel/same-owner `^確認^` confirmation; negative divisors are not blocked to preserve legacy integer-option behavior | implemented behind `MHCAT_FEATURE_ECONOMY_COIN_RESET_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_COIN_RESET`; requires gateway, Guild Messages intent, and Message Content intent; test only against disposable staging data |

### Implemented Low-risk Logging Config and Event Slices

| Command name | Type | Category | Inputs | Permissions | Cooldown | Mongo reads | Mongo writes | Discord API calls | External API calls | Response type | Error behavior | Legacy edge cases | Feature parity status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `刪除訊息` | slash | 管理系統 | required integer `刪除數量`, optional user `使用者` | `ManageMessages` (`8192`); `Administrator` (`8`) when count is above 200 | metadata `30`; not centrally enforced in legacy | none | none | fetch recent channel messages; delete matching messages within Discord's 14-day deletion window | none | deferred ephemeral edit | missing Manage Messages or missing Administrator returns the legacy red permission embed; count above 1000 returns legacy red maximum embed; Discord cleanup errors return generic red error | legacy user-filter mode repeatedly fetched recent messages with delayed loops; Go paginates recent messages for the selected user and deletes single-message batches with the single-delete API | implemented behind `MHCAT_FEATURE_MESSAGE_CLEANUP_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_MESSAGE_CLEANUP`; no Mongo writes |
| `刪除資料` | slash + select | 管理系統 | select target: `加入訊息`, `離開訊息`, `審核日誌`, `統計系統`, `自動聊天`, `驗證設置`, `聊天經驗設置`, `語音經驗設置`, `私人頻道設置` | `ManageMessages` (`8192`) | metadata `10`; not centrally enforced in legacy | selected guild config row existence | deletes selected guild rows from `join_messages`, `leave_messages`, `loggings`, `numbers`, `chats`, `verifications`, `text_xp_channels`, `voice_xp_channels`, or `tickets` | deferred public warning prompt, ephemeral select result | none | legacy destructive warning embed with `delete-data` select and ephemeral text result | missing selected config returns legacy text `<a:Discord_AnimatedNo:1015989839809757295> **| 你沒有設定過這個選項!**`; success returns `<a:green_tick:994529015652163614> **| 成功刪除該設定!**` | legacy collector restricted select usage to the invoking user; Go cannot recover collector state from the static legacy custom ID, so it requires Manage Messages on the component and deletes duplicate guild rows for the selected singleton target | implemented behind `MHCAT_FEATURE_DELETE_DATA_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_DELETE_DATA`; test only against disposable staging config data |
| `set-log-channel` / `設置日誌` | slash + select | 管理系統 | required channel option `channel` / `頻道`, channel types text/news; one-to-four logging toggles | public command definition; `ManageMessages` (`8192`) checked at runtime | metadata `10`; not centrally enforced in legacy | event paths read `loggings` by `{guild}` with Mongoose-compatible `channel_id`/Boolean scalar coercion | typed `channel_id`/Boolean config writes update duplicate guild rows then upsert if absent; global slash middleware optionally increments `all_use_counts` once | public defer/edit, exact yellow prompt/red denial, owner-scoped ten-minute select; independently gated message edit/delete, channel topic/permission, and voice join/leave embeds | none | exact legacy prompt/footer/options/colors and selected-value text; menu options remain non-default | cross-user/expired selects return safe errors; orphaned `loggin_create` requests a rerun; message-delete action `72` requires exact target+channel, channel action `11` uses its first entry unfiltered, null topics render `null`, and moves/mute/deafen emit nothing | cached pre-edit author/old channel/old member data is authoritative; everything after the unterminated `LoggingSystem.js:364` comment is inactive; Go safely handles duplicate writes, audit failures, overwrite IDs, mention suppression, and uncached REST targets | parity-audited behind independent config/message/channel/voice gates; exact contract, ownership, smoke, rollback, and usage behavior are recorded in [logging parity contract](48-logging-config.md) |

### Implemented Gacha Prize-list and Prize-delete Slices

| Command name | Type | Category | Inputs | Permissions | Cooldown | Mongo reads | Mongo writes | Discord API calls | External API calls | Response type | Error behavior | Legacy edge cases | Feature parity status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `自動聊天頻道` | slash + MessageCreate runtime | 實用工具 | required channel `頻道` (text/news); runtime message content | config `ManageMessages`; runtime human users | metadata `10`; paid runtime uses legacy 10-second guard | `chats`, `chatgpt_gets`, `chatgpts` | config writes `chats`; paid runtime transactionally debits `chatgpt_gets.price` and patches legacy `chatgpts` fields | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when enabled; runtime sends typing/replies/transient warnings | external ChatGPT worker inferred | green config embed; local 1-5 second reply; paid fixed 10-second worker reply | permission denial; bot/DM/wrong-channel ignored; paid input/output `@` is blocked; busy source/warning messages are deleted | local preserves missing/negative/malformed fallback and zero silence; paid preserves UTF-16 price, 10/40-second timing and conversation IDs while atomically preventing charge-without-request | config, local fallback, and paid handoff are separately gated; paid additionally requires transaction-capable Mongo and explicit ownership confirmation |
| `自動聊天頻道刪除` | slash | 實用工具 | none | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | none | deletes `chats` by `{guild}` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | none | green `自動聊天系統` delete success embed | missing config returns red `你沒有設定過，我不知道要刪除甚麼!` | legacy deletes one found row; Go deletes duplicate `{guild}` rows to keep rollback-consistent data; deleting config also makes the local fallback ignore future events | implemented behind `MHCAT_FEATURE_AUTOCHAT_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_AUTOCHAT_CONFIG`; fallback performs no Mongo writes |
| `automatic-notification` | slash + modal + selects | 自動通知 | required channel `channel`/`頻道` (text/news); direct five-field cron or simplified weekday/hour/minute selections | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | `cron_sets` by `{guild}` for the 10-schedule limit | inserts pending `cron_sets` row with `cron:null` and `message:null`; direct or simplified completion sets `cron` and rollback-compatible `message` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true`; best-effort setup preview message send; separately gated recurring channel sends | none | legacy five-field setup modal; invalid/`取消`/`cancel` cron opens the public weekday/hour/minute wizard; completion shows generated id and preview divider | permission denial, invalid color, empty message/title/content, direct intervals below 15 minutes, missing pending row, stale wizard, and cross-user selection return safe legacy-style errors | Go preserves the five-minute collector lifetime and visible select options but uses versioned owner-scoped state IDs instead of ambiguous `week_menu`/`hour_menu`/`min_menu`; random colors are resolved once during setup to match the persisted legacy payload | config UI implemented behind `MHCAT_FEATURE_AUTO_NOTIFICATION_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_AUTO_NOTIFICATION_CONFIG`; recurring sends are separately implemented behind `MHCAT_FEATURE_AUTO_NOTIFICATION_DELIVERY_ENABLED`, Gateway, and scheduler lease gates |
| `自動通知列表` | slash | 自動通知 | none | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | `cron_sets` by `{guild}` | deletes abandoned pending setup drafts where `cron` is null or missing | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | public reply with optional guild-name lookup | none | legacy list embed title/description and schedule rows | permission denial returns red legacy tutorial error | legacy deletes `cron === null` drafts but could still render them in the same response; Go filters pending drafts from the response while cleaning them up | implemented behind `MHCAT_FEATURE_AUTO_NOTIFICATION_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_AUTO_NOTIFICATION_CONFIG`; recurring delivery is controlled independently |
| `自動通知刪除` | slash | 自動通知 | required string `id` | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | none | deletes one `cron_sets` row by `{guild,id}` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | none | green `自動通知系統` delete success embed | missing id returns the legacy red missing-id tutorial embed | preserves legacy one-row delete semantics for duplicate IDs; the delivery worker reloads `{guild,id}` before every send, so deleting the selected row prevents future sends even before the next reconciliation | implemented behind `MHCAT_FEATURE_AUTO_NOTIFICATION_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_AUTO_NOTIFICATION_CONFIG`; recurring delivery is controlled independently |
| `防詐騙網址` | slash | 群組防護 | none | public command definition; `ManageMessages` (`8192`) at runtime | metadata `10`; runtime check in command | `good_webs` by `{guild}` | typed patch/upsert of `guild`, `open` across duplicate matches | global slash middleware records one attempt when enabled | deferred public edit | none | exact green fraud-alert success embed with `true`/`false` text | exact permission denial; backend failures use controlled error UI | Mongoose Boolean reads, duplicate-safe writes, bot scanning, warning newline, ownership, and rollback are locked in [77-anti-scam.md](77-anti-scam.md) | parity-audited behind independent config and deletion gates |
| `詐騙網址回報` | slash | 群組防護 | required string `網址` | public | metadata `10`; not enforced in legacy | `not_a_good_webs` by escaped `web` regex | global slash usage only; no catalog writes | deferred public edit; awaited webhook POST | Discord webhook | exact green fraud-alert success embed with `成功回報<raw-url>` | exact invalid/duplicate UI; backend failures use controlled error UI | pinned `is-url@1.2.4`, raw text, escaped regex, sanitized webhook, scalar reads, and rollback are locked in [77-anti-scam.md](77-anti-scam.md) | parity-audited behind report/runtime/webhook gates |
| `扭蛋獎池查詢` | slash | 扭蛋系統 | none | public | metadata `10`; not centrally enforced in legacy | `gifts` by `{guild}`, `gift_changes` by `{guild}` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit; optional guild info lookup for guild name | none | legacy-style public embed with prize fields | empty pool returns legacy red embed title | preserves misspelled `gift_chence` field and legacy value text, including the visible apostrophe before the counter emoji; Go splits more than 25 prize fields into multiple embeds to avoid Discord API failure | implemented behind `MHCAT_FEATURE_GACHA_PRIZE_LIST_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_PRIZE_LIST`; shop writes are separately gated |
| `扭蛋` | slash | 扭蛋系統 | optional string `連抽` with choices `5`, `11`, `17`, `23` | public | metadata `1`; not centrally enforced in Go | `coins`, `gifts`, `gift_changes` | updates matching `coins` rows, decrements/deletes drawn auto-delete `gifts` rows, and relies on the separately gated Go global slash middleware to increment `all_use_counts` | deferred public response; loading GIF follow-up held for 8.5 seconds and then edited to the final embed | best-effort prize-code DM and one notification-channel winner embed per non-air draw | legacy draw result embed with footer `如扭中的獎品有獎品代碼，將會私訊給您!` | missing balance, empty pool, and insufficient coins return legacy red ephemeral follow-ups | preserves paid/actual draw mapping, weighted air result, and per-draw pool reload/depletion; intentionally applies one inventory decrement/delete per drawn prize instead of legacy duplicate async decrement loops | implemented behind `MHCAT_FEATURE_GACHA_DRAW_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_DRAW`; use isolated staging data only until duplicate audits and transaction/DM policy are reviewed |
| `扭蛋獎池增加` | slash | 扭蛋系統 | required string `獎品名稱`, required number `機率`, optional string `獎品代碼`, optional boolean `自動刪除`, optional integer `獎品數量`, optional integer `給予硬幣` | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | `gifts` count by `{guild}`, exact-name duplicate lookup by `{guild,gift_name}` | inserts one `gifts` row with fields `guild`, `gift_name`, `gift_code`, `gift_chence`, `auto_delete`, `gift_count`, `give_coin`; legacy global slash dispatcher increments `all_use_count`, Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred ephemeral edit | none | green legacy success embed title `<a:green_tick:994529015652163614>設置成功` with six inline fields | missing permission, duplicate prize, overfull pool, invalid count, and numeric-looking name greater than `200` return red legacy errors | preserves exact untrimmed names/codes, JavaScript numeric name coercion, zero-chance BSON `null`, legacy defaults (`auto_delete=true`, `gift_count=1`, `give_coin=0`), and the legacy `data.length > 24` pool guard; inserts no indexes and does not draw prizes, decrement inventory, send DMs, or mutate user coin balances | implemented behind `MHCAT_FEATURE_GACHA_PRIZE_CREATE_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_PRIZE_CREATE`; draw/shop writes are separately gated |
| `扭蛋獎品編輯` | slash | 扭蛋系統 | required string `獎品名稱`, optional number `機率`, optional string `獎品代碼`, optional boolean `自動刪除`, optional integer `獎品數量`, optional integer `給予硬幣` | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | `gifts` one exact-name row by `{guild,gift_name}` | deletes the matched row and inserts one merged `gifts` row with fields `guild`, `gift_name`, `gift_code`, `gift_chence`, `auto_delete`, `gift_count`, `give_coin`; legacy global slash dispatcher increments `all_use_count`, Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred ephemeral edit | none | green legacy success embed title `<a:green_tick:994529015652163614>編輯成功成功` with six inline fields | missing permission, missing prize, invalid count, and numeric-looking name greater than `200` return red legacy errors | preserves exact untrimmed names/codes, JavaScript numeric name coercion, and legacy merge quirks: false `自動刪除` does not override existing true, omitted/zero chance and coin keep existing values, omitted/zero count defaults the saved count to `1`; no transaction/rollback around delete-plus-insert | implemented behind `MHCAT_FEATURE_GACHA_PRIZE_EDIT_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_PRIZE_EDIT`; draw/shop writes are separately gated |
| `扭蛋獎池刪除` | slash | 扭蛋系統 | required string `獎品名稱` | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | none | deletes one exact-name `gifts` row by `{guild,gift_name}`; legacy global slash dispatcher increments `all_use_count`, Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | none | green legacy success embed title `<a:green_tick:994529015652163614>成功刪除!`, exact untrimmed description `獎品名:<gift_name>` | missing prize returns red `找不到這個獎品!`; missing permission returns red legacy Manage Messages error | preserves legacy `findOne(...); data.delete()` one-row behavior for duplicate prize names; does not decrement inventory counts, mutate coins, send DMs, or touch `gift_changes` | implemented behind `MHCAT_FEATURE_GACHA_PRIZE_DELETE_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_PRIZE_DELETE`; draw/shop writes are separately gated |
| `查看餘額` | slash | 管理系統 | none | public | metadata `10`; not centrally enforced in legacy | `chatgpt_gets` by `{guild}` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true`; no feature writes | deferred ephemeral edit | none | green author embed `伺服器目前剩於餘額: <price>` with legacy success gif icon | missing balance row displays `0` | preserves legacy typo `剩於`; reads mixed string/numeric `price` values for rollback compatibility; does not itself enable either auto-chat runtime | implemented behind `MHCAT_FEATURE_BALANCE_QUERY_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_BALANCE_QUERY`; no command-path chatbot writes |
| `兌換` | slash | 管理系統 | required string `代碼` | public | metadata `60`; not centrally enforced in legacy | `codes` by `{code}`, `chatgpt_gets` by `{guild}` | deletes `codes` row, credits `chatgpt_gets.price`; legacy global slash dispatcher increments `all_use_count`, Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred ephemeral edit | none | green author embed `成功兌換代碼!` with success gif and balance-query footer | missing code returns red `找不到這個代碼!`; expired code returns the legacy support-server text and does not consume the code | preserves legacy 7-day expiry check against `codes.time`; Go updates duplicate `chatgpt_gets` rows instead of delete+insert as a rollback-compatible cleanup fix; does not itself enable either auto-chat runtime | implemented behind `MHCAT_FEATURE_REDEEM_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_REDEEM`; local fallback and paid handoff use separate gates |
| `統計系統查詢` | slash | 統計系統 | none | public | metadata `10`; not centrally enforced in legacy | none | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | public reply | none | exact legacy static stats help embed | none found | preserves exact text, whitespace, public visibility, and random color | parity-audited behind `MHCAT_FEATURE_STATS_QUERY_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_STATS_QUERY`; rename worker is a separate event-only gate |
| `統計系統創建` | slash | 統計系統 | required string `統計頻道類型` with choices `文字頻道`, `語音頻道`; optional string `統計選項` with choices `頻道數量`, `文字頻道數量`, `語音頻道數量` | `ManageMessages` (`8192`) | metadata `30`; runtime check in command | `numbers` by `{guild}` | creates the category/base counters or one optional counter; writes legacy field names with 38 explicit null fields | deferred public follow-up; first creation sends and edits loading follow-up | Discord channel create | exact green loading/success titles, channel names, and permission overwrites | exact permission/type/missing-option/duplicate/unknown-option red titles | preserves cached member/bot counts, stale v14 channel-type results, cache-only parent lookup, and voice stored-value bug; Go awaits writes and atomically upserts/updates | parity-audited behind create runtime/sync gates plus gateway and Guild Members intent; rename worker is separate |
| `統計身分組人數` | slash | 統計系統 | required string `統計頻道類型` with choices `文字頻道`, `語音頻道`; required role `身分組` | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | `numbers` by `{guild}`; `role_numbers` by `{guild,role}` | creates a text/voice role counter and replaces `{guild,role}` config | deferred public loading follow-up edited to result | Discord channel create | exact loading/success embed, channel name, and text/voice overwrites | exact permission, invalid type, and missing-base red embeds | preserves cached role counts, `@everyone`, cache-only parent lookup, and document shape; Go awaits writes and removes duplicate config rows | parity-audited behind role runtime/sync gates plus gateway and Guild Members intent; rename worker is separate |
| `統計系統刪除` | slash | 統計系統 | none | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | `numbers` by `{guild}` | deletes legacy `numbers` rows by `{guild}` | deferred public edit | none | exact green success embed with first matched category ID | exact permission and missing-config red embeds | neither implementation deletes Discord channels; Go awaits deletion and removes all duplicate `{guild}` rows instead of one | parity-audited behind `MHCAT_FEATURE_STATS_DELETE_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_STATS_DELETE`; rename worker is separate |
| `聊天經驗` | slash | 經驗系統 | optional user `玩家` | public | metadata `10`; runtime immediately returns before XP reads | none | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | none | red legacy replacement embed text: 該指令即將被移除，請使用 `/我的檔案` 進行替代 | selected user is ignored because legacy returns before lookup | preserves the current disabled command behavior only; old rank-card/profile rendering remains outside scope | implemented behind `MHCAT_FEATURE_XP_PROFILE_DISABLED_COMMANDS_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_XP_PROFILE_DISABLED_COMMANDS`; XP accrual/profile card not-started |
| `語音經驗` | slash | 經驗系統 | optional user `玩家` | public | metadata `10`; runtime immediately returns before XP reads | none | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | none | red legacy replacement embed text: 該指令即將被移除，請使用 `/我的檔案` 進行替代 | selected user is ignored because legacy returns before lookup | preserves the current disabled command behavior only; old rank-card/profile rendering remains outside scope | implemented behind `MHCAT_FEATURE_XP_PROFILE_DISABLED_COMMANDS_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_XP_PROFILE_DISABLED_COMMANDS`; voice-state XP accrual/profile card not-started |
| `聊天排行榜` | slash + legacy buttons | 經驗系統 | none | public | metadata `10`; not centrally enforced in legacy | `text_xps` by `{guild}` | reads all guild text XP profiles and sorts by the legacy accumulated XP formula | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | public loading reply, then edit with PNG attachment | page buttons `[user]{page}text_rank` and viewer-target `[user]text_rank {page}` | orange legacy loading embed followed by `user-info.png` titled `聊天經驗排行榜` | missing component target user returns ephemeral red `找不到資料!請於幾分鐘後重試!` embed | preserves legacy rank math quirks, page size 10, `text_rank` disabled page-label ID, and viewer-target page calculation | implemented behind `MHCAT_FEATURE_XP_RANK_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_XP_RANK`; XP accrual/profile card/automatic role assignment not-started; see `docs/71-xp-rank.md` |
| `語音排行榜` | slash + legacy buttons | 經驗系統 | none | public | metadata `10`; not centrally enforced in legacy | `voice_xps` by `{guild}` | reads all guild voice XP profiles and sorts by the legacy accumulated XP formula, including the legacy `/3` leaderboard total | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | public loading reply, then edit with PNG attachment | page buttons `[user]{page}voice_rank` and viewer-target `[user]voice_rank {page}`; disabled page label remains `text_rank` for parity | orange legacy loading embed followed by `user-info.png` titled `語音經驗排行榜` | missing component target user returns ephemeral red `找不到資料!請於幾分鐘後重試!` embed | preserves legacy voice leaderboard math quirks despite voice XP adjustment using different math elsewhere | implemented behind `MHCAT_FEATURE_XP_RANK_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_XP_RANK`; voice-state XP accrual/profile card/automatic role assignment not-started; see `docs/71-xp-rank.md` |
| `經驗值改變` | slash | 經驗系統 | subcommands `聊天經驗改變`, `語音經驗改變`; required user `使用者`; required integer `經驗值` | `KickMembers` (`2`) | metadata `10`; runtime check in command | `text_xps` or `voice_xps` by `{guild,member}` | upserts one profile with legacy string `xp` and misspelled string `leavel`; voice-profile inserts also set `leavejoin: leave` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | none | green `<:xp:990254386792005663> 經驗系統` success embed with adjusted user and amount | permission denial returns red `你需要有\`踢出用戶\`才能使用此指令`; invalid options/repository errors return safe generic red error | preserves the legacy level math, including zero-delta behavior, and suppresses mention pings in the success embed as a safety fix | implemented behind `MHCAT_FEATURE_XP_ADMIN_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_XP_ADMIN`; XP accrual/profile cards/automatic role assignment not-started |
| `經驗值重製` | slash + message confirmation | 經驗系統 | subcommands `聊天經驗重製`, `語音經驗重製`, `重製個人語音經驗`, `重製個人聊天經驗`; individual subcommands require user `使用者` | guild owner only | metadata `10`; runtime checks guild owner ID | `text_xps` or `voice_xps` by `{guild,member}` or `{guild}` | individual subcommands delete matching profile rows; full-server subcommands arm a 60-second same-channel/same-owner `^確認^` confirmation before deleting guild XP rows | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit; full reset sends channel message after confirmation | message-create event with Message Content intent for confirmation | warning content matches legacy; individual success content says `成功清除<@user>的聊天/語音經驗`; full reset success embed title says `成功刪除伺服器內所有聊天/語音經驗` | non-owner, missing individual profile, wrong confirmation, and empty voice-guild data return legacy red embeds; empty text-guild data preserves the legacy success response | preserves the legacy one-shot confirmation requirement; Go deletes duplicate matching profile rows for rollback-compatible cleanup, sends confirmation results as channel messages rather than Discord message replies, and replaces an earlier pending reset for the same owner/channel | implemented behind `MHCAT_FEATURE_XP_RESET_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_XP_RESET`; requires gateway, Guild Messages intent, and Message Content intent; XP accrual/profile cards/automatic role assignment not-started; see `docs/70-xp-reset.md` |
| `聊天經驗設定` | slash | 經驗系統 | required channel `頻道` (text/news), optional string `訊息`, optional string `顏色` | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | none | `text_xp_channels` by `{guild}`, fields `channel`, `color`, `message`; clears `background` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit; optional preview send to invoking channel | none | green `聊天經驗系統` success embed and legacy preview separator | invalid color returns red legacy error; permission denial returns red legacy error | legacy delete+insert can race and leaves mention pings in preview; Go uses duplicate-tolerant update/upsert and suppresses allowed mentions in preview | implemented behind `MHCAT_FEATURE_TEXT_XP_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_TEXT_XP_CONFIG`; text XP accrual has a separate event gate; profile cards not-started |
| `聊天經驗刪除` | slash | 經驗系統 | none | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | none | deletes `text_xp_channels` by `{guild}` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | none | green `聊天經驗系統` success embed with `成功刪除!` | missing config returns red `你本來就沒有對聊天經驗設定喔!` | legacy deletes one found row; Go deletes duplicate `{guild}` rows to keep rollback-consistent data | implemented behind `MHCAT_FEATURE_TEXT_XP_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_TEXT_XP_CONFIG`; text XP accrual has a separate event gate; profile cards not-started |
| `聊天經驗身分組設定` | slash + legacy buttons | 經驗系統 | subcommands `增加`, `刪除`, `設定查詢`; level integer, role, delete-when-not boolean | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | `chat_roles` by `{guild}`; role hierarchy via Discord adapter | delete+insert by `{guild,leavel,role}`, delete by same key; fields `guild`, string `leavel`, `role`, `delete_when_not` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit; query pages update via `<page>text_leave_role` buttons | none | green add/delete embeds; query embed lists 12 rows/page with legacy emojis and prev/next buttons | over-119 configs, unassignable role, missing delete, and permission denial return legacy red errors | preserves misspelled `leavel`; Go deletes duplicate matching rows before insert and deletes duplicate matches on remove | implemented behind `MHCAT_FEATURE_XP_ROLE_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_XP_ROLE_CONFIG`; automatic role assignment/profile cards not-started |
| `語音經驗設定` | slash | 經驗系統 | required channel `頻道` (text/news), optional string `訊息`, optional string `顏色`, optional string `背景` | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | none | `voice_xp_channels` by `{guild}`, fields `channel`, `color`, `message`; clears `background` because legacy exposed but did not save it | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit; optional preview send to invoking channel | none | green `語音經驗系統` success embed and legacy preview separator | invalid color returns red legacy error; permission denial returns red legacy error | legacy delete+insert can race and leaves mention pings in preview; Go uses duplicate-tolerant update/upsert and suppresses allowed mentions in preview | implemented behind `MHCAT_FEATURE_VOICE_XP_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_VOICE_XP_CONFIG`; voice-state XP accrual/profile cards not-started |
| `語音經驗刪除` | slash | 經驗系統 | none | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | none | deletes `voice_xp_channels` by `{guild}` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | none | green `語音經驗系統` success embed with `成功刪除!` | missing config returns red `你本來就沒有對語音經驗設定喔!` | legacy deletes one found row; Go deletes duplicate `{guild}` rows to keep rollback-consistent data | implemented behind `MHCAT_FEATURE_VOICE_XP_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_VOICE_XP_CONFIG`; voice-state XP accrual/profile cards not-started |
| `語音經驗身分組設定` | slash + legacy buttons | 經驗系統 | subcommands `增加`, `刪除`, `設定查詢`; level integer, role, delete-when-not boolean | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | `voice_roles` by `{guild}`; role hierarchy via Discord adapter | delete+insert by `{guild,leavel,role}`, delete by same key; fields `guild`, string `leavel`, `role`, `delete_when_not` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit; query pages update via `<page>voice_leave_role` buttons | none | green add/delete embeds; query embed lists 12 rows/page with legacy emojis and prev/next buttons | over-119 configs, unassignable role, missing delete, and permission denial return legacy red errors | preserves misspelled `leavel`; Go deletes duplicate matching rows before insert and deletes duplicate matches on remove | implemented behind `MHCAT_FEATURE_XP_ROLE_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_XP_ROLE_CONFIG`; voice XP accrual/profile cards/automatic role assignment not-started |
| `語音包廂設置` | slash + voice-state event | 語音包廂 | required channel `語音頻道` (voice/stage), required raw string `設定頻道名稱`, required boolean `是否予許房主上鎖`, optional integer `設定人數上限` | public definition; runtime `ManageMessages` (`8192`) | metadata `10`; runtime check in command | none | `voice_channels` by `{guild,ticket_channel}`, fields `guild`, `ticket_channel`, `limit`, `name`, nullable `parent`, `lock`; Mongoose-compatible scalar reads; dynamic state in `voice_channel_ids`; optional lock seed in `lock_channels` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | voice-state room create/move/DM when gateway Voice State events are enabled | green legacy setup embed title `<a:green_tick:994529015652163614> &#124; 成功進行設定` and description `<:Voice:994844272790610011> 你成功對語音包廂進行\`設定\`` | invalid negative or over-99 limit returns `必須為1-99的整數!`; permission denial returns red legacy error | omitted and explicit `0` both mean unlimited; raw name whitespace is retained and only the first `{name}` is replaced; human or bot trigger joins use the trigger's current parent/overwrites, grant owner Manage Channels/Roles, save state/nullable lock rows, and move the member | parity-audited behind `MHCAT_FEATURE_VOICE_ROOM_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_VOICE_ROOM_CONFIG`; dynamic events additionally require Gateway and Voice State intent; see `docs/72-voice-room-config.md` |
| `語音包廂刪除` | slash + voice-state cleanup | 語音包廂 | required channel `頻道或類別` (voice/stage/category) | public definition; runtime `ManageMessages` (`8192`) | metadata `10`; runtime check in command | none | selected type-2 voice channels delete `voice_channels` by `{guild,ticket_channel}`; every other type, including stage, deletes by `{guild,parent}`; empty-room cleanup deletes `voice_channel_ids` and matching `lock_channels` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | voice-state empty dynamic channel delete | trigger success embed title `<a:green_tick:994529015652163614>成功進行刪除`, category success title `成功進行刪除`; both use the legacy delete descriptions | missing type-2 trigger returns `你沒有對這個頻道做出設定過喔!`; missing category/stage branch returns `你沒有對這個類別沒有設定喔!` | slash delete removes config rows only and removes all duplicate matches; stage channels intentionally follow the legacy category branch; voice-state cleanup deletes empty tracked dynamic channels and state rows | parity-audited behind `MHCAT_FEATURE_VOICE_ROOM_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_VOICE_ROOM_CONFIG`; dynamic cleanup additionally requires Gateway and Voice State intent; see `docs/72-voice-room-config.md` |
| `上鎖頻道` | slash + voice-state prompt/modal | 語音包廂 | optional raw string `密碼`; modal input `anser` | public definition; current voice-channel lock owner for command; prompt is scoped to joining user | metadata `10`; no default permission in legacy command definition | `lock_channels` by `{guild,channel_id}` for the invoking member's current voice channel or joined locked channel; Mongoose-compatible scalar/array reads | dynamic room creation seeds nullable lock rows; command replaces the row with raw nullable `lock_anser`, `owner`, nullable `text_channel`, and BSON-array `ok_people`; correct modal submit uses `$addToSet` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred ephemeral edit; voice-state prompt send; modal defer/edit | prompt send, member disconnect, DM | fixed prompt/success/error colors are `#53FF53`/`#EA0000`; DMs use per-message random colors; omitted password displays/stores null while explicit empty string remains distinct | not in voice returns `你不在一個語音包廂!`; missing lock row returns `你不在語音包廂或該語音包廂不支援設密碼!`; non-owner returns `只有包廂房主可以設定!`; wrong modal password returns the legacy red error plus channel link; a deleted room returns `很抱歉，該包廂可能已被刪除!` | exact password/owner/allowed-user matching; generated prompt IDs bind channel, user, and an exact 60-second deadline because legacy `lock_start` depended on process-local collector state | parity-audited behind `MHCAT_FEATURE_VOICE_ROOM_LOCK_ENABLED`, Gateway, Voice State intent, and `MHCAT_COMMAND_SYNC_INCLUDE_VOICE_ROOM_LOCK`; config gate is needed only to seed new lock rows; see `docs/72-voice-room-config.md` |
| `加入身份組設置` | slash | 加入設置 | required role `身分組`, optional string `給人還是給機器人` choices `all_user`/`all_bot`/`all_member` | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | role hierarchy through Discord adapter only | `join_roles` insert by `{guild,role}`, fields `guild`, `role`, `give_to_who` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | none | green `🪂 加入身分組系統` success embed with legacy `<@roleID>` formatting | duplicate role returns red `很抱歉，這個身分組已經被註冊了，請重試!`; unassignable role returns legacy hierarchy error | legacy find-then-save can race; Go uses upsert `$setOnInsert`; member-add role assignment remains disabled | implemented behind `MHCAT_FEATURE_JOIN_ROLE_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_JOIN_ROLE_CONFIG`; Guild Members intent/event assignment not-started |
| `加入身份組刪除` | slash | 加入設置 | required role `身分組` | `ManageMessages` (`8192`) | metadata `10`; runtime check in command | none | deletes `join_roles` by `{guild,role}` | legacy global slash dispatcher increments `all_use_count`; Go global slash middleware increments `all_use_counts` when `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` | deferred public edit | none | green `🪂 加入身分組系統` delete embed with legacy `<@roleID>` formatting | missing config returns red `找不到這個身份組!` | legacy deletes one found row; Go deletes duplicate `{guild,role}` rows to keep data rollback-consistent | implemented behind `MHCAT_FEATURE_JOIN_ROLE_CONFIG_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_JOIN_ROLE_CONFIG`; member-add side effects not-started |

## Prefix / Message Command Matrix

| Command name | Type | Category | Inputs | Permissions | Cooldown | Mongo reads | Mongo writes | Discord API calls | External API calls | Response type | Error behavior | Legacy edge cases | Feature parity status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `MHCAT restart now` | message | admin/restart | exact message text | hardcoded admin user IDs | none enforced | none | none | `client.shard.respawnAll`, message reply | none found | channel reply | raw message content | requires `MessageContent`; should become owner-only slash or ops action by ADR | not-started |
| `請MHCAT開始執行重啟任務!` | message | restart signal | exact message text from hardcoded bot/webhook path | hardcoded bot ID | none enforced | none | none | restart flow/reply | hardcoded webhook sends this on `beforeExit` | channel reply | possible loop if copied | hardcoded webhook must not be copied | not-started |

## Component Matrix

| Component ID / pattern | Type | Source file | Inputs | Permissions | Mongo reads | Mongo writes | Discord API calls | Response behavior | Error behavior | Feature parity status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `poll_*`, `poll_menu`, `see_result`, `menu_choose`; `mhcat:v1:poll:*` | button/select | `events/poll.js`, Go typed router | poll choice/menu/result | poll owner/rules from DB | `polls` | conditional vote/toggle updates | edit/update/chart/export | ephemeral defer/edit/update | legacy broad matching, `poll_menu` choice collision, versioned rollback constraint | parity-audited behind `MHCAT_FEATURE_POLLS_ENABLED`; exact migration, UI, data, smoke, and rollback behavior is in the [poll parity contract](75-poll.md) |
| `tic`, `del` | button | `events/Ticket System.js`, `events/yicket system.js` | ticket open/close | exact-name owner or `ManageMessages` close context | `tickets` | channel state | create/delete channels, send welcome | legacy reply/channel deletion | permission/rate-limit risk; failed welcome must not strand a channel | parity-audited behind `MHCAT_FEATURE_TICKETS_ENABLED`; exact routes, overwrites, compensation, and owner-close fix are in the [ticket parity contract](74-ticket.md) |
| `lock_start`, `<channel>anser`; Go prompt `mhcat:v1:voice_lock:prompt:*` | button/modal | voice lock events | password/channel ID | prompt is scoped to joining user | `lock_channel` | `ok_people` append on correct answer | disconnect locked-channel joiner; DM instructions | modal/edit | custom ID spoofing risk | in-progress: existing locked-channel joins emit the legacy-style prompt, generated prompt buttons open the legacy `<channel>anser` modal, and modal answers append `ok_people`; old orphaned `lock_start` cannot recover channel state and returns retry |
| `<captcha>verification`, `<captcha>ver`; new `mhcat:v1:verification:*:state=<id>` | button/modal | verification/welcome/modal | captcha input | public | `verification` | member role state | role add/nickname | modal/reply | legacy captcha custom ID parsing risk; new IDs intentionally hide captcha answer | in-progress: legacy IDs decoded, new Go messages use state IDs, role/nickname completion implemented behind explicit flags |
| `cron_set*`, `week_menu`, `hour_menu`, `min_menu` | buttons/selects/modal | cron events/modal | schedule/message/channel | `ManageMessages` | `cron_set` | `cron_set` | scheduled sends | modal/edit | scheduler lifecycle risk | in-progress: legacy `cron_set*` modal submit plus simplified weekday/hour/minute flow are implemented; Go-generated selects use `mhcat:v1:cron:<week|hour|minute>:state=<id>` because the raw legacy IDs collide with birthdays; recurring sends are implemented behind the independent Gateway/write/lease delivery gates |
| `loggin_create` | select | `slashCommands/管理系統/create_logging.js` | selected log types | message-scoped legacy collector plus `ManageMessages` setup command | none | `logging` | edit/update logging prompt | update/edit | legacy custom ID has no channel payload and depended on process-local collector state | in-progress: new Go messages use versioned `mhcat:v1:logging:configure:c=<channel>`; old orphaned `loggin_create` is recognized and returns a safe rerun error |
| `text_rank`, `voice_rank`, `coin_rank` page IDs | buttons | `events/rank.js` | page/user | public | XP/coin collections | none | image render/edit | update/edit | render-before-ACK risk | in-progress: legacy text/voice XP rank and coin-rank pagination IDs are parsed, routed, and render legacy-style PNG updates behind their rank feature gates |
| `<user>_sing...`, `<user>my-profile`, `botinfoupdate`, `shardinfoupdate` | buttons | economy/profile/rank events | user/year/month | user-scoped | coin/sign/profile data | possible profile/sign state | image render/edit | update/edit | user spoofing/collision risk | in-progress: sign-page and profile legacy IDs supported |
| `roleadd*`, `*add`, `*delete` | buttons | `events/btn.js` | role IDs | member role context | `btn`/role configs | role membership | add/remove roles | update/reply | component ID collision risk | parity-audited: exact modal/panel/IDs, visibility, cached hierarchy ordering, REST writes for uncached members, errors, one-gate ownership, and reliability differences are locked in the [role-selection parity contract](73-role-selection.md) |
| `<digits>lotter`, `<digits>lottersearch`, `<digits>lotterrestart`, `<digits>lotterstop` | buttons | lottery handlers | lottery ID/user | legacy duplicate/cap/date/role entry precedence; owner/guild-owner management, with Manage Messages fallback for ownerless legacy rows | `lotters` | atomic participant append; `end:true` | one aggregate winner channel send on positive-count reroll | ephemeral defer/edit; nonpositive reroll preserves deferred no-op | creation remains disabled; Go requires generated numeric IDs, exact participant IDs, atomic/end guards, destructive-action authorization, malformed-row skipping, explicit winner mentions, and a 50-winner cap | parity-audited behind `MHCAT_FEATURE_LOTTERY_COMPONENTS_ENABLED`; search/export formatting, entry, reroll, and stop are covered; creation remains disabled |
| game IDs: `yesssss`, `nooooo`, `main_get_card`, `user_get_card`, `lookmenumber`, `teach21point`, `thansize`, knowledge answer text | buttons | economy game | wager/game state | participant-scoped | `coin` | `coin` | game message edits | update/reply | collector/timer/race risk | implemented behind `MHCAT_FEATURE_ECONOMY_GAME_ENABLED`: legacy game buttons and answer IDs route through serialized session claims; knowledge/blackjack generation timers settle forfeits and remove components; process restart recovery remains open |

## Modal Matrix

| Modal ID / pattern | Source file | Inputs | Validation | Mongo reads | Mongo writes | Response behavior | Error behavior | Feature parity status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `*anser` | voice lock modal | password | compare stored password | `lock_channel` | appends `ok_people` with `$addToSet` | legacy success/error edit with channel link | ad hoc | in-progress: modal submit route and generated prompt-button flow are implemented behind voice-room lock runtime flags; legacy orphaned `lock_start` cannot recover channel state |
| `cron_set*` | cron setup | schedule/message | direct cron expression or simplified weekday/hour/minute selections; color and non-empty content/embed validation | `cron_set` pending row | completes pending `cron_set` row with `cron` and rollback-compatible `message`; sends preview best-effort | defer/edit/update | five-minute process-local wizard state mirrors the legacy collector; versioned IDs avoid birthday collisions | implemented behind the auto-notification config gate; recurring sends are implemented behind the independent delivery/Gateway/lease gates |
| `join_msg*`, `leave_msg*` | join/leave setup | message/embed fields | length/color/channel validation | `join_message`/`leave_message` | config writes | edit | ad hoc | partial: legacy leave modal/config/preview routing is implemented; the current join command preserves its dashboard redirect, so old join-modal writes are not generated |
| `nal` + first field `ticketcolor`; `mhcat:v1:ticket:setup:*` | ticket setup | ticket config/panel | color and required text; versioned ID carries category/admin role | `tickets` | versioned path creates config after validation; legacy `nal` publishes only | public defer/edit plus panel send | stale submissions receive duplicate error; failed panel send rolls back its exact row | parity-audited; see [ticket parity contract](74-ticket.md) |
| `ann*`, `announcement_yes/no` | announcement | message/embed confirmation | color/text validation | `guild`/`ann_all_set` | announcement sends/config writes | send/edit | ad hoc | parity-audited: legacy modal migration plus owner/guild-scoped versioned confirm/cancel state with an exact six-second deadline; raw shared button IDs remain rejected; see [76-announcement.md](76-announcement.md) |
| `<captcha>ver`; new `mhcat:v1:verification:answer:state=<id>` | verification | captcha answer | compare expected answer | `verification` | none directly; role/nickname side effects through Discord | defer/edit success or red error embed | wrong answer, missing config, missing/unassignable role, owner nickname error | in-progress: legacy and versioned modal IDs supported behind explicit verification flow flag |
| work captcha/job-name buttons | work | captcha/task name | captcha/name validation | `work_*` | work state/coin writes | reply/edit | versioned IDs for new messages, legacy-compatible parser | in-progress |

## Event Matrix

| Event | Source file | Trigger | Inputs | Mongo reads | Mongo writes | Discord API calls | External side effects | Error behavior | Shard/idempotency concern | Feature parity status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `ready` | `events/ready.js`, `handler/slash_commands.js` | shard ready | client/application state | command state | application command state | command create/delete, webhook/status | webhook/logging | raw logs | every shard mutates global commands | intentionally replaced: bot startup never mutates commands; the explicit dry-run-first command-sync CLI owns scoped registration |
| `interactionCreate` | `SlashCommands.js`, `btn.js`, `modal.js`, `poll.js`, `rank.js`, ticket events | slash/component/modal/select | interaction payload | many | many | replies/edits/modals/channels/roles | rendering/translation as needed | broad try/catch, raw errors | multiple listeners can double-handle | implemented through one typed parser/router/dispatcher with feature-gated route registration and centralized response state |
| `messageCreate` | `index.js`, `MessageCreate.js`, `Chatbot.js`, `ann_message.js`, `text_xp.js`, `safe_server.js` | message in guild/DM | content/author/channel | chat/XP/scam/config | XP/chatgpt/etc. | reply/delete/send/restart | chatbot/scam/report | ad hoc | requires `MessageContent` | announcement relay and anti-scam deletion are parity-audited in [76-announcement.md](76-announcement.md) and [77-anti-scam.md](77-anti-scam.md); other event families remain independently gated |
| `voiceStateUpdate` | `voice_xp.js`, `voice_create.js`, `LoggingSystem.js` | voice join/leave/move | old/new state | voice config/XP/locks | voice XP/session/channel state | channel create/delete/move/disconnect/log | none found | ad hoc | intervals and restart risk | in-progress: dynamic room creation/move/delete with `voice_channel_ids`, lockable-room seed rows, existing passworded lock-channel join prompt/disconnect/DM, gated voice XP `leavejoin` session tracking, the legacy 30-second voice XP runtime loop, startup reconciliation for existing joined rows, and legacy-style voice join/leave logging are implemented; the active legacy logging handler intentionally emits nothing for channel moves or mute/deafen-only updates |
| `guildMemberAdd/Remove` | `welcome.js` | member join/leave | member/guild | join/leave/role/verification/create_hours | role/nickname state | role add, nickname, kick, send logs | webhook/logging | ad hoc | requires `GuildMembers`; account-age must run before join-role/welcome handlers | in-progress for join-role assignment, welcome/leave-message delivery, and gated account-age kick/logging |
| `messageReactionAdd/Remove` | `message_reaction.js` | reaction role | message/reaction/member | `message_reaction` | member role state | add/remove roles | none found | ad hoc | remove payload omits member; first-seen post-restart bot detection is best-effort | parity-audited behind one `MHCAT_FEATURE_ROLE_SELECTION_ENABLED` ownership gate, Gateway, and Guild Message Reactions intent; see the [role-selection parity contract](73-role-selection.md) |
| `messageUpdate/Delete`, `channelUpdate` | `LoggingSystem.js` | audit/log events | old/new message/channel | `logging` | none | log channel sends/audit fetch | logging | ad hoc | privacy/logging risk | message update/delete implemented behind `MHCAT_FEATURE_LOGGING_MESSAGE_EVENTS_ENABLED`, Gateway, Guild Messages, and Message Content with cached old/deleted message data; channel topic/permission updates implemented behind `MHCAT_FEATURE_LOGGING_CHANNEL_EVENTS_ENABLED`, Gateway, and cached old channel data |

## Cron Job Matrix

| Job | Source file | Schedule source | Mongo reads | Mongo writes | Discord API calls | External side effects | Shard coordination | Error behavior | Feature parity status |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| Persisted automatic notifications | `handler/cron.js`, cron command/modal | `cron_set.cron` | `cron_set`, `mhcat_scheduler_locks` | schedule metadata and lease state | channel sends | none | Mongo lease `auto-notification-delivery`; entries reconciled at most every 30 seconds | app shutdown hook | implemented behind `MHCAT_FEATURE_AUTO_NOTIFICATION_DELIVERY_ENABLED`, Gateway, and scheduler lease gates; five-field cron runs at fixed UTC+8 named `Asia/Taipei`, active rows are reloaded before each send, and Node `handler/cron.js` must be disabled before rollout |
| Daily coin/work reset | `handler/cron.js` | 00:00 Asia/Taipei, shard 0 | `gift_change`, `coin`, `work_set`, `work_user`, `mhcat_scheduler_locks` | reset `coin.today`, refill/clamp `work_user.energi` | none | none | lease `daily-reset` acquired per run by both CLI apply and recurring worker | app shutdown hook/CLI cleanup | implemented behind independent write/scheduler/Gateway/lease gates; five-field `0 0 * * *` runs at fixed UTC+8 named `Asia/Taipei`; Node `handler/cron.js` must be disabled before Go ownership |
| Work completion payout | `handler/gift.js` | every minute | `work_user`, `coin`, `gift_change` | increment balance and reset work state | none for payout path | none | shard-0 guard only; no distributed lease | ad hoc | implemented behind disabled-by-default gates: one-shot dry-run/apply and every-minute worker share a lease; atomic markers make Go retries idempotent; production ownership smoke pending |
| Channel status rename | `handler/channel_status.js` | delayed first run, then every 20 minutes | `Number`, `role_number` | cached guild/member/channel data | channel rename | none | legacy per-shard/process overlap risk | replace first stored count or replace whole name; deleted roles become zero | parity-audited behind `MHCAT_FEATURE_STATS_RENAME_WORKER_ENABLED`, gateway, and Guild Members intent; Go serializes and awaits work, batches base updates, continues after bad role rows, and must not overlap Node ownership |
| Birthday notifications/roles | `handler/gift.js` | intended minute job | `birthday`, `birthday_set` | intended allow/role changes | send birthday messages/roles | none | inactive/commented | inactive | intentional decision required |

## Permission Model

- Some admin behavior uses hardcoded user IDs in `index.js`.
- Slash command permissions are checked ad hoc inside handlers.
- Declared `UserPerms`/permission metadata is not a reliable runtime policy.
- Some commands advertise permissions but lack runtime checks.
- No consistent `DefaultMemberPermissions` policy was found during registration.
- New design must centralize owner/admin/user/bot permission checks, role hierarchy checks, and `guildOnly` behavior.

## Cooldown Model

Cooldown fields exist on slash command modules but central enforcement was not found. Treat legacy cooldown values as intended behavior metadata. Enforcing them consistently is a user-visible behavior change and must be documented.

## Error Behavior

- Global unhandled rejection and exception handlers log stack/error details.
- `functions/error_send.js` can send raw errors to Discord channels.
- Many callback errors are ignored or thrown from async callbacks.
- New design must use safe user-facing errors and redacted structured logs.

## User-Facing Output Notes

Preserve command names, option names, public/ephemeral behavior, embeds, emojis, buttons, select menus, modals, and error text where safe.

Likely required intentional changes:

- Remove hardcoded webhook behavior.
- Centralize and enforce permissions/cooldowns.
- Prevent raw error disclosure.
- Avoid unsafe regex from user input.
- Defer long interactions consistently.

## Mongo Side Effects

Common collections: `guild`, `logging`, `Number`, `role_number`, `all_use_count`, join/leave/verification/ticket/voice config, XP/economy/work/gacha/poll/lottery/birthday/cron/warning/chat collections. See `02-mongo-collection-catalog.md`.

## Discord Side Effects

Sends/edits/deletes messages, bulk deletes, creates/deletes/renames channels, edits permission overwrites, adds/removes roles, kicks/bans/moves/disconnects members, sends DMs/webhooks, fetches audit logs, registers/deletes global application commands.

## External API Side Effects

Google Translate, captcha generation, canvas/chart rendering, Excel/text attachments, Discord CDN/image loading, dashboard/docs links, PM2/Docker runtime. No direct OpenAI API call found.

## Behavior Parity Checklist

See `docs/10-feature-parity-checklist.md`.
