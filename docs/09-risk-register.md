# Risk Register

Status: Phase 1.5 Gate B review.

## Secret Exposure Response

Sensitive value found:

- Type: Discord webhook URL
- Location: `MHCAT/index.js:253`
- Redacted identifier: `<redacted:discord-webhook:id=1085973338238754848:last4=qDRN>`
- Severity: High
- Immediate action: revoke/rotate the webhook in Discord if it still exists, then audit Discord webhook logs for unexpected sends.
- Legacy source action: do not modify the legacy file as part of this refactor.
- Go refactor action: no hardcoded webhook, token, Mongo URI, dashboard URL, bot owner ID, bot admin ID, or operator bot ID may be introduced. All operator IDs and webhooks must come from validated config, be redacted in logs, and be covered by a hardcoded-secret scan.

| Risk | Area | Evidence | Impact | Likelihood | Mitigation | Owner | Status |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Hardcoded Discord webhook exists in legacy | Security | `MHCAT/index.js:253` contains `<redacted:discord-webhook:id=1085973338238754848:last4=qDRN>` | Secret leakage and webhook abuse if copied | High | Revoke/rotate immediately; never copy to Go; require env-only webhooks and secret scan | Security Agent | open |
| Hardcoded admin/operator IDs | Security/Ops | `MHCAT/index.js` restart/admin list and restart bot ID are hardcoded | Unauthorized/incorrect ops control | High | Move to `BOT_OWNER_IDS`/`BOT_ADMIN_IDS`; central owner policy; remove hardcoded operator IDs from Go | Security Agent | open |
| Message Content intent required for parity | Discord/Privacy | restart, XP, chatbot, announcements, anti-scam, logging use content | Privileged intent burden and privacy exposure | High | Feature-flag intent; replace restart; document retained content features | DiscordGo Specialist | open |
| Global slash registration on every shard | Discord/Reliability | `handler/slash_commands.js` runs on ready | Rate limits and command state races | High | Move to `mhcat-tools commands register --dry-run/--apply` | DiscordGo Specialist | open |
| Mongo production indexes unknown | MongoDB | no explicit indexes; `autoIndex:false` | Slow queries and unsafe unique assumptions | High | `check-indexes`, index catalog, dry-run apply, duplicate audits | MongoDB Data Agent | open |
| Live Mongo audit not yet executed | MongoDB/Rollout | No Mongo URI/database was present in the Phase 1.5 environment | Production collection names, counts, indexes, duplicates, and mixed types remain unverified | High for production writes; Low for Wave 1 skeleton | Run `tools/mongo-audit-readonly.mjs` before repository writes, unique indexes, or canary | MongoDB Data Agent | open |
| Production collection names unverified | MongoDB | no explicit collection names; inferred Mongoose defaults | Go may read/write wrong collection | Medium | `list-collections` audit before writes; collection-name tests | MongoDB Data Agent | open |
| Mongo catalog still contains singular scaffold names | MongoDB/Data | Read-only system agents confirmed production collections are Mongoose-pluralized (`coins`, `text_xps`, `polls`, etc.) while the Wave 3 `DefaultCollectionCatalog()` used singular placeholders for the high-risk subset | Stateful repositories could read/write the wrong collection if implemented from the scaffold catalog | High | Platform Wave B expanded `DefaultCollectionCatalog()` to all 47 legacy model files, corrected Mongoose collection names, and added collection-name contract tests | MongoDB Data Agent | resolved |
| Dashboard shares bot collections | Integration/Data | Local `mhcat-dashboard` writes `join_messages`, `guilds`, and `work_somethings`; backup API exports guild collections | Schema or collection rename can break dashboard settings/backup | High | Treat shared collections as public contracts; additive changes only until dashboard is updated; document dashboard impact | Rollout Agent | open |
| Non-atomic economy/XP/poll/gacha writes | Data correctness | read-modify-write and raw updates across legacy | Lost updates, duplicate rewards, inventory corruption | High | Poll Wave A moves vote add/remove into repository methods with conditional writes and fake contract tests; economy/XP/gacha still require atomic repository methods, concurrency tests, and index audits | Go Architecture Agent | partially mitigated |
| `join_message.enable` unique Boolean | MongoDB/Data | schema has `unique:true`; autoIndex disabled | Index build failure or invalid uniqueness | Medium | Do not create; audit and ADR before any unique index | MongoDB Data Agent | open |
| Dashboard `work_something.guild` unique declaration conflicts with bot behavior | MongoDB/External | Dashboard schema declares `guild` unique, but bot treats `{guild,name}` as work identity | Index build could block multiple work jobs per guild | High | Do not copy dashboard unique declaration; audit duplicates; use `{guild:1,name:1}` candidate only after duplicate audit | MongoDB Data Agent | open |
| Unsafe regex from user input | Security/Performance | safe/report URL paths use user strings in `$regex` | ReDoS/incorrect matching | High | Normalize domains; escape regex; fuzz tests; ADR for behavior change | Security Agent | open |
| Raw errors/user content in logs/responses | Security/Privacy | global handlers and `error_send`; logging deleted messages | Secret/user data leakage | High | Redacted structured logs and safe user-facing errors | Security Agent | open |
| Component custom ID collisions/spoofing | Discord/Security | broad `includes()` matching, generic modal ID `nal`, user/admin-controlled raw IDs | Wrong handler, unauthorized actions | High | Legacy compatibility decoders plus typed versioned IDs; collision tests; no broad router in Go | DiscordGo Specialist | open |
| Core feature registry imports Discord abstractions | Architecture | `internal/core/features` imports `internal/discord/commands` and `internal/discord/interactions`; `internal/core/services/utility/help.go` imports command metadata | The core layer is not fully domain-only even though it still avoids DiscordGo and MongoDB driver imports | Medium | Move feature module registry and help command metadata out of `internal/core` or introduce a driver-agnostic command metadata port in a dedicated architecture cleanup slice | Architecture Owner | open |
| Interaction timeout risk | Discord/Reliability | long DB/render/fetch paths without consistent defer | Failed interactions | High | Responder state machine; defer long paths; ACK tests | DiscordGo Specialist | open |
| Cron duplicate execution | Reliability/Data | jobs loaded per process; partial shard-0 guard | Duplicate messages/resets/payouts | High | Mongo scheduler lease primitive and read-only-by-default diagnostic CLI now exist, but no recurring job is wired yet; each job still needs lease usage plus job-specific idempotency/fencing tests before enablement | Performance Agent | partially mitigated |
| Node and Go double-handle during rollout | Rollout/Data | same token/guild would process same events | Duplicate writes/side effects | High | Staging token; shadow read-only; canary exclusive ownership | Rollout Agent | open |
| Unbounded member cache / large member fetches | Performance | `GuildMemberManager.maxSize: Infinity`; poll/rank fetches | Memory pressure and REST bursts | Medium | bounded cache, REST queue, projections, pagination | Performance Agent | open |
| Rendering inline on interaction path | Performance/Discord | rank/profile/poll/canvas/chart generation | timeouts and high memory | Medium | worker pools, timeout, defer, benchmarks | Performance Agent | open |
| Poll commands synced before runtime side effects are enabled | Feature parity/Ops | Poll command definitions can now be included in staging command sync while runtime is disabled by default | Users could see `/投票創建` without working handlers if command sync and runtime flags are not paired | High | Runtime requires `MHCAT_FEATURE_POLLS_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_POLLS=true`; staging preflight and scripts reject unpaired poll sync/runtime flags | Poll Feature Owner | mitigated |
| Poll result export parity gap | Feature parity/UI | Poll Wave A returned text result embeds but did not render the legacy pie chart attachment or `poll_info.xlsx` export | Users expecting chart/Excel output got partial parity until Wave B | Medium | Poll Wave B adds `file.jpg`, `discord.txt`, and `poll_info.xlsx` attachments with artifact tests; staging smoke still required before production enablement | Poll Feature Owner | mitigated |
| Poll export member lookup can exceed interaction budget | Performance/Discord | Result/export paths may need member tags for many voters; Discord REST lookup per vote would risk rate limits and handler timeout | Slow or failed result/export interactions on large polls | Medium | Member lookup uses a bulk/cache port and anonymous export skips lookup; future large-poll renderer/export worker should be considered before production rollout | Poll Feature Owner | partially mitigated |
| Economy query command synced before runtime repository is enabled | Feature parity/Ops | `/代幣查詢` now has a local command definition but runtime remains disabled by default | Users could see a slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_ECONOMY_QUERY_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_QUERY=true`; staging preflight and scripts reject unpaired flags | Economy Feature Owner | mitigated |
| Economy sign-in write enabled before duplicate/reset readiness | Data correctness/Ops | `/簽到` writes `coins` and `sign_lists`; production duplicate audits and unique `{guild,member}` indexes are not yet approved, and daily reset now exists only as an explicit one-shot tool | Duplicate first sign-ins, coin/calendar drift, or daily-mode permanent signed-in state if reset ownership is missed | High | `MHCAT_FEATURE_ECONOMY_SIGNIN_ENABLED=false` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_SIGNIN=false` by default; staging preflight/scripts reject unpaired sync/runtime flags; `mhcat-economy-reset` is dry-run by default and requires `MHCAT_JOBS_DAILY_RESET_ENABLED=true --apply`; do not enable production sign-in until duplicate audit, unique-index plan, and reset ownership are complete | Economy Feature Owner | open |
| Economy settings write enabled before shared-consumer rollout | Data correctness/Ops | `/coin-related-settings` writes `gift_changes`, which is read by gacha, sign-in, daily reset, and XP reward paths | A staging/prod operator could change economy config while downstream Go consumers are still partial or while duplicate singleton rows exist | High | `MHCAT_FEATURE_ECONOMY_SETTINGS_ENABLED=false` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_SETTINGS=false` by default; staging preflight/scripts reject unpaired flags; repository updates all duplicate `gift_changes` rows for a guild and does not create indexes; do not enable production until duplicate audit and shared-consumer rollout plan are complete | Economy Feature Owner | open |
| Economy coin-admin write enabled before duplicate/ownership readiness | Data correctness/Ops | `/代幣增加` can create and adjust `coins` rows directly for a member while production duplicate audits, unique `{guild,member}` index strategy, and Node/Go feature ownership are not approved | Duplicate balance rows, unexpected rollback reads, or real user balance mutation during parity testing | High | `MHCAT_FEATURE_ECONOMY_COIN_ADMIN_ENABLED=false` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_COIN_ADMIN=false` by default; staging preflight/scripts reject unpaired flags; repository rejects negative and over-`999999999` balances and updates duplicate `{guild,member}` rows together; test only against disposable staging data until duplicate audits and production ownership are reviewed | Economy Feature Owner | open |
| Economy RPS write enabled before duplicate/ownership readiness | Data correctness/Ops | `/剪刀石頭布` mutates existing `coins` rows for one member and intentionally preserves the legacy no-cap win behavior while production duplicate audits, unique `{guild,member}` index strategy, and Node/Go feature ownership are not approved | Duplicate balance rows, unexpected rollback reads, or real user balance mutation during parity testing | High | `MHCAT_FEATURE_ECONOMY_RPS_ENABLED=false` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_RPS=false` by default; staging preflight/scripts reject unpaired flags; repository rejects missing/insufficient balances and updates duplicate `{guild,member}` rows together; test only against disposable staging data until duplicate audits and production ownership are reviewed | Economy Feature Owner | open |
| Economy game write/session behavior enabled before ownership readiness | Data correctness/Ops | `/代幣遊戲` mutates two players' `coins` rows for `21點`, `知識王`, and `比大小`, and uses process-local component session state while production duplicate audits, unique `{guild,member}` index strategy, and Node/Go feature ownership are not approved | Duplicate balance rows, stale sessions after restart, missed legacy timeout edits, unexpected rollback reads, or real user balance mutation during parity testing | High | `MHCAT_FEATURE_ECONOMY_GAME_ENABLED=false` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_GAME=false` by default; staging preflight/scripts reject unpaired flags; repository rejects missing/insufficient balances and updates duplicate `{guild,member}` rows together; handler validates the invited user before debiting to avoid the legacy challenger-accept debit bug; test only against disposable staging data until duplicate audits, timeout behavior, and production ownership are reviewed | Economy Feature Owner | open |
| Economy coin reset enabled without message-content readiness | Feature parity/Data | `/代幣重製` preserves the legacy `^確認^` message confirmation before full-guild coin deletion/division | Operators could expose a command that arms a destructive confirmation prompt but never receives the confirming message, or enable privileged message-content access without review | High | Config validation and staging preflight require `MHCAT_DISCORD_ENABLE_GATEWAY=true`, `MHCAT_DISCORD_GUILD_MESSAGES_INTENT=true`, and `MHCAT_DISCORD_MESSAGE_CONTENT_INTENT=true`; docs require isolated staging fixtures | Economy Feature Owner | mitigated |
| Economy coin reset deletes or rewrites production balances before ownership review | Data correctness/Ops | `/代幣重製` can delete all guild `coins` rows or divide every balance while production duplicate audits, backups, and Node/Go economy ownership remain partial | Accidental production enablement could permanently erase or rewrite user currency balances | High | Disabled by default; requires guild owner check plus legacy confirmation; command sync and runtime gates remain staging-only until economy ownership, backups, duplicate audits, and rollback plans are approved | Economy Feature Owner | open |
| Daily reset one-shot applied without ownership review | Data correctness/Ops | `mhcat-economy-reset --apply` can reset `coins.today` and refill/clamp `work_users.energi`; recurring lease-backed scheduler is not implemented | Double reset, missed reset, or work-energy drift if Node and Go both own reset timing | Medium | Keep dry-run default; require `MHCAT_JOBS_DAILY_RESET_ENABLED=true --apply`; do not wire into bot startup; implement scheduler lease before recurring background runs | Economy Feature Owner | open |
| Work payout one-shot applied without ownership/idempotency review | Data correctness/Ops | `mhcat-work-payout --apply` can increment `coins.coin` and reset due `work_users.state`; Node `handler/gift.js` may still own the minute loop, and the first Go slice does not yet add a per-job payout marker or transaction | Duplicate coin payouts after process crash between coin increment and state reset, or double ownership if Node and Go run together | High | Dry-run default; require `MHCAT_JOBS_WORK_PAYOUT_ENABLED=true`, `MHCAT_SCHEDULER_LEASE_ENABLED=true`, `MHCAT_SCHEDULER_LEASE_OWNER`, and `--apply`; use scheduler lease for Go operator contention; do not wire into bot startup; audit duplicates for `coins`, `work_users`, and `gift_changes`; decide transaction/payout marker before recurring scheduler | Economy Feature Owner | open |
| Work command synced before scheduler/data ownership is complete | Feature parity/Ops | The Go refactor now exposes a staged `打工系統` command definition with dashboard redirect, interface/detail/start UI, setup/delete, and energy grants, but recurring payout/reset ownership and full crash idempotency are not complete | Users could start/administer work jobs while Node.js or an operator process still owns payout/reset timing, causing double or missed payouts if rollout is not controlled | High | Disabled by default; runtime requires `MHCAT_FEATURE_WORK_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_WORK=true`; staging preflight rejects unpaired sync/runtime flags; start/admin writes are wired only through explicit work repositories; do not production-sync until scheduler ownership, duplicate audits, and payout idempotency are reviewed or a partial rollout is approved | Work Feature Owner | open |
| Warning-history command synced before runtime gate is paired | Feature parity/Ops | `/警告紀錄` now has a managed command definition but runtime remains disabled by default | Users could see a slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_WARNINGS_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_WARNINGS=true`; staging preflight rejects unpaired flags | Moderation Feature Owner | mitigated |
| Warning-settings command synced before runtime gate is paired | Feature parity/Ops | `/警告設定` now has a managed command definition but runtime remains disabled by default | Users could see a config-write slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_WARNING_SETTINGS_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_WARNING_SETTINGS=true`; staging preflight and scripts reject unpaired flags | Moderation Feature Owner | mitigated |
| Warning-settings mistaken for full warning moderation runtime | Feature parity/Ops | `/警告設定` writes legacy escalation thresholds but enforcement only happens when the separately gated `/警告` runtime is enabled | Operators could assume setting the threshold alone enables enforcement | Medium | README/runbooks document the command as config-only; it writes only `errors_sets` and has separate flags from warning-history/removal/warning-issue actions | Moderation Feature Owner | open |
| Warning-removal commands synced before runtime gate is paired | Feature parity/Ops | `/警告清除` and `/警告全部清除` now have managed command definitions but runtime remains disabled by default | Users could see warning-removal slash commands that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_WARNING_REMOVAL_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_WARNING_REMOVAL=true`; staging preflight and scripts reject unpaired flags | Moderation Feature Owner | mitigated |
| Warning-removal mistaken for full warning moderation runtime | Feature parity/Ops | `/警告清除` and `/警告全部清除` remove legacy warning rows but do not create warnings or run threshold actions | Operators could assume warning moderation is complete after enabling removal | Medium | README/runbooks document the commands as removal-only; they mutate only `warndbs` and use separate flags from history/settings/warning-issue runtime | Moderation Feature Owner | open |
| Warning-issue command synced before runtime gate is paired | Feature parity/Ops | `/警告` now has a managed command definition but runtime remains disabled by default | Users could see a warning-write slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_WARNING_ISSUE_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_WARNING_ISSUE=true`; staging preflight and scripts reject unpaired flags | Moderation Feature Owner | mitigated |
| Warning-issue enabled against non-disposable data | Safety/Data correctness | `/警告` appends `warndbs` entries and can kick or ban when `errors_sets` thresholds are met for existing warning records | Staging or production users could receive real warnings, DMs, kicks, or bans during parity testing | High | Disabled by default; README/runbooks require isolated staging warning fixtures and paired command/runtime flags; handler preserves Manage Messages and role hierarchy checks; threshold actions are explicit side effects | Moderation Feature Owner | open |
| Message-cleanup command synced before runtime gate is paired | Feature parity/Ops | `/刪除訊息` now has a managed command definition but runtime remains disabled by default | Users could see a destructive slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_MESSAGE_CLEANUP_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_MESSAGE_CLEANUP=true`; staging preflight and scripts reject unpaired flags | Moderation Feature Owner | mitigated |
| Message cleanup enabled outside disposable staging channels | Safety/Discord data loss | `/刪除訊息` deletes recent channel messages and can inspect up to 1000 requested messages; Discord refuses messages older than 14 days | Staging or production operators could delete real user messages during parity testing | High | Disabled by default; requires Manage Messages and Administrator above 200; README/runbooks require disposable staging channels; Go filters messages older than 14 days and writes no Mongo data | Moderation Feature Owner | open |
| Delete-data command synced before runtime gate is paired | Feature parity/Ops | `/刪除資料` now has a managed command definition but runtime remains disabled by default | Users could see a destructive config-delete slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_DELETE_DATA_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_DELETE_DATA=true`; staging preflight and scripts reject unpaired flags | Moderation Feature Owner | mitigated |
| Delete data enabled against non-disposable config rows | Safety/Data loss | `/刪除資料` deletes guild-scoped legacy config rows for join/leave messages, logging, stats, autochat, verification, text/voice XP, or ticket settings | Staging or production operators could remove real server configuration during parity testing | High | Disabled by default; requires Manage Messages; README/runbooks require disposable staging config data; repository deletes only the selected target collection for the current guild and creates no indexes | Moderation Feature Owner | open |
| Legacy warning-history member-cache crash | Feature parity/Bug fix | `warnings.js:47` dereferences `interaction.guild.members.cache.get(w.moderator).user.tag` without a nil check | A warning entry whose moderator is not cached can crash or fail the command | Medium | Go warning-history handler falls back to the stored moderator ID when member tag lookup fails; test covers fallback behavior | Moderation Feature Owner | mitigated |
| Warning-history permission metadata was not centrally enforced | Security/Privacy | `warnings.js:29` declares `UserPerms: '訊息管理'`, but the command body does not enforce it directly | Moderation data could be exposed if the legacy dispatcher permission path is bypassed or incomplete | Medium | Go handler explicitly requires Manage Messages before reading `warndbs`; this is an intentional security/metadata-consistency fix | Moderation Feature Owner | mitigated |
| Translate external provider latency/availability | Feature parity/Reliability | Legacy `/翻譯` calls `@vitalets/google-translate-api` and only logs provider errors, leaving the loading embed stuck | Users may see slow/failing translation responses, especially under the default interaction timeout | Medium | Go `/翻譯` defers immediately, uses a provider port with HTTP timeout, extends runtime timeout when translate is enabled, and returns a safe red error embed on provider failure | Utility Feature Owner | mitigated |
| Auto-chat config commands synced before runtime gate is paired | Feature parity/Ops | `/自動聊天頻道` and `/自動聊天頻道刪除` now have managed command definitions while runtime remains disabled by default | Users could see config commands that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_AUTOCHAT_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_AUTOCHAT_CONFIG=true`; staging preflight and scripts reject unpaired flags | Utility Feature Owner | mitigated |
| Auto-chat config mistaken as full chatbot implementation | Feature parity/Privacy | Current Go slice only writes/deletes `chats`; legacy `events/Chatbot.js` requires Message Content, reads/writes `chatgpts` and `chatgpt_gets`, and may rely on an external worker | Operators could assume chat replies or ChatGPT handoff are active after syncing config commands | Medium | Document config-only scope; keep Gateway, Guild Messages, and Message Content disabled by default; implement chatbot runtime only after worker ownership, privacy, rate-limit, and mention-safety review | Utility Feature Owner | open |
| Auto-chat config writes before duplicate singleton audit | MongoDB/Data | Legacy `chat` has singleton `{guild}` semantics without a confirmed unique index, and legacy delete+insert could leave duplicate rows | Future chatbot runtime may read an arbitrary duplicate config; unique index creation would fail before cleanup | High | Go updates/deletes all duplicate `{guild}` rows and creates no index; production rollout still requires duplicate audit before unique index or chatbot runtime enablement | Utility Feature Owner | open |
| Redeem command synced before runtime gate is paired | Feature parity/Ops | `/兌換` now has a managed command definition while runtime remains disabled by default | Users could see a slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_REDEEM_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_REDEEM=true`; staging preflight and scripts reject unpaired flags | Utility Feature Owner | mitigated |
| Redeem mistaken as ChatGPT/autochat runtime | Feature parity/Privacy | `/兌換` credits `chatgpt_gets.price`, but legacy `events/Chatbot.js` still requires Message Content, `chatgpts`, and an external worker path | Operators could assume ChatGPT replies are active after syncing redeem and balance commands | Medium | Document balance-credit-only scope; keep Gateway, Guild Messages, and Message Content disabled by default; implement chatbot runtime only after worker ownership, privacy, rate-limit, and mention-safety review | Utility Feature Owner | open |
| Redeem code consume and credit are non-transactional | Data correctness | Legacy deletes the `codes` row before writing `chatgpt_gets`; Go preserves that non-transactional order for rollback-compatible behavior | A crash or Mongo error after code deletion can lose a redeem code without crediting balance | High | Keep disabled by default and staging-gated; smoke only with disposable codes; add transaction or repair/replay tooling before production enablement | Utility Feature Owner | open |
| Auto-notification config commands synced before runtime gate is paired | Feature parity/Ops | `automatic-notification`, `/自動通知列表`, and `/自動通知刪除` now have managed command definitions while runtime remains disabled by default | Users could see commands that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_AUTO_NOTIFICATION_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_AUTO_NOTIFICATION_CONFIG=true`; staging preflight and scripts reject unpaired flags | Notifications Feature Owner | mitigated |
| Auto-notification setup/list/delete mistaken as full scheduler implementation | Feature parity/Scheduler | Current Go slice creates direct-cron `cron_sets` rows, sends setup previews, lists schedules, and deletes schedules; legacy `handler/cron.js` still owns recurring sends through per-process CronJob instances | Operators could assume recurring notification sends or the simplified cron select-menu flow are active after syncing the config commands | Medium | Document setup/list/delete-only scope; keep simplified cron select-menu components, Message Content, recurring scheduler ownership, and recurring sends disabled until a separate lease-backed scheduler slice is designed and staged | Notifications Feature Owner | open |
| Auto-notification setup writes disposable schedule data | Data correctness/Ops | `automatic-notification` now creates pending `cron_sets` rows, completes them with direct cron/message payloads, and sends a preview to the interaction channel | Accidental staging or production sync could write non-disposable schedule config or send an unexpected preview message | High | Disabled by default; require paired staging runtime and command-sync flags; use disposable channels and staging data only; do not production-sync until scheduler ownership, duplicate policy, and preview/send safety are reviewed | Notifications Feature Owner | open |
| Auto-notification pending draft cleanup differs from legacy same-response rendering | Feature parity/Data | Legacy `/自動通知列表` calls `delete()` for `cron === null` setup drafts but still maps the already-loaded array into the response, while Go filters pending drafts before rendering and deletes null/missing `cron` drafts | A staging operator may see fewer rows than the legacy response when abandoned setup drafts exist | Low | Treat filtering as an intentional cleanup/safety fix; tests cover pending draft filtering and cleanup, and docs call out the behavior | Notifications Feature Owner | mitigated |
| Anti-scam config command synced before runtime gate is paired | Feature parity/Ops | `/防詐騙網址` now has a managed command definition while runtime remains disabled by default | Users could see a slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_ANTI_SCAM_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_ANTI_SCAM_CONFIG=true`; staging preflight rejects unpaired flags | Safety Feature Owner | mitigated |
| Anti-scam config mistaken as full URL protection | Feature parity/Privacy | `/防詐騙網址` only toggles `good_webs.open`; scam URL deletion runs only when the separate `MHCAT_FEATURE_ANTI_SCAM_MESSAGE_DELETE_ENABLED` runtime, Gateway, Guild Messages, and Message Content flags are explicitly enabled | Operators could assume scam URL deletion is active after syncing the toggle command alone, or enable privileged message deletion without isolated staging review | Medium | Document config/runtime separation; keep Gateway, Guild Messages, and Message Content disabled by default; test deletion only against disposable staging `good_webs` and `not_a_good_webs` rows before production rollout | Safety Feature Owner | open |
| Anti-scam config writes before duplicate singleton audit | MongoDB/Data | Legacy `good_web` has singleton `{guild}` semantics without a confirmed unique index, and legacy delete+insert could leave duplicate rows | Future URL scanner may read an arbitrary duplicate config; unique index creation would fail before cleanup | High | Go updates all duplicate `{guild}` rows and creates no index; production rollout still requires duplicate audit before unique index or scanner enablement | Safety Feature Owner | open |
| Logging-config command synced before runtime gate is paired | Feature parity/Ops | `/set-log-channel` now has a managed command definition while runtime remains disabled by default | Users could see a slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_LOGGING_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_LOGGING_CONFIG=true`; staging preflight and scripts reject unpaired flags | Logging Feature Owner | mitigated |
| Logging event emitters expose private content and rate-limit pressure | Security/Privacy/Discord | Legacy `LoggingSystem.js` can log deleted/updated message content, channel changes, and voice movement | Private content or sensitive audit details could be sent to configured channels, and event bursts could hit Discord rate limits | High | Message update/delete emitters are default-off behind `MHCAT_FEATURE_LOGGING_MESSAGE_EVENTS_ENABLED`, require Gateway/Guild Messages/Message Content, use cached message data, and suppress mentions; channel/voice emitters remain disabled until per-event redaction, permission, and rate-limit tests are written | Logging Feature Owner | open |
| Gacha prize-list command synced before runtime gate is paired | Feature parity/Ops | `/扭蛋獎池查詢` has a managed command definition while runtime remains disabled by default | Users could see a slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_GACHA_PRIZE_LIST_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_PRIZE_LIST=true`; staging preflight and scripts reject unpaired flags | Gacha Feature Owner | mitigated |
| Gacha draw command synced before runtime gate is paired | Feature parity/Ops | `/扭蛋` has a managed command definition while runtime remains disabled by default | Users could see a slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_GACHA_DRAW_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_DRAW=true`; staging preflight and scripts reject unpaired flags | Gacha Feature Owner | mitigated |
| Gacha prize-create command synced before runtime gate is paired | Feature parity/Ops | `/扭蛋獎池增加` has a managed command definition while runtime remains disabled by default | Users could see a slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_GACHA_PRIZE_CREATE_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_PRIZE_CREATE=true`; staging preflight and scripts reject unpaired flags | Gacha Feature Owner | mitigated |
| Gacha prize-edit command synced before runtime gate is paired | Feature parity/Ops | `/扭蛋獎品編輯` has a managed command definition while runtime remains disabled by default | Users could see a slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_GACHA_PRIZE_EDIT_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_PRIZE_EDIT=true`; staging preflight and scripts reject unpaired flags | Gacha Feature Owner | mitigated |
| Gacha prize-delete command synced before runtime gate is paired | Feature parity/Ops | `/扭蛋獎池刪除` has a managed command definition while runtime remains disabled by default | Users could see a slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_GACHA_PRIZE_DELETE_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_PRIZE_DELETE=true`; staging preflight and scripts reject unpaired flags | Gacha Feature Owner | mitigated |
| Gacha prize-create applied to non-disposable data | Data correctness/Ops | `/扭蛋獎池增加` inserts a `gifts` row and can change the live prize pool shown to users | A staging or production operator could add invalid or unintended live prize definitions | High | Runtime and sync flags default false; docs require disposable staging fixtures; do not enable production until gacha prize admin workflow, backups, duplicate-name policy, and chance/count validation policy are reviewed | Gacha Feature Owner | open |
| Gacha prize-edit applied to non-disposable data | Data correctness/Ops | `/扭蛋獎品編輯` deletes one `gifts` row and inserts a merged replacement without transaction rollback | A staging or production operator could overwrite a live prize definition or lose the old row if insertion fails after deletion | High | Runtime and sync flags default false; docs require disposable staging fixtures; do not enable production until gacha prize admin workflow, backups, duplicate-name policy, rollback policy, and chance/count validation policy are reviewed | Gacha Feature Owner | open |
| Gacha prize-delete applied to non-disposable data | Data correctness/Ops | `/扭蛋獎池刪除` removes one `gifts` row by `{guild,gift_name}` and does not have an undo path | A staging or production operator could remove a live prize definition unexpectedly | High | Runtime and sync flags default false; docs require disposable staging fixtures; do not enable production until gacha prize admin workflow, backups, and duplicate-name policy are reviewed | Gacha Feature Owner | open |
| Gacha draw/inventory writes remain non-atomic | Data correctness | `/扭蛋` reads and mutates coin balances, prize inventory, prize-code DMs, and notification-channel messages behind disabled gates, and `/代幣商店` mutates `ghps`, `coins`, role assignments, and prize-code DMs behind disabled gates | Lost prizes, negative stock, duplicate prize codes, coin drift, or confusing prize delivery failures | High | Runtime and sync flags default false; Go intentionally applies one inventory decrement/delete per drawn auto-delete prize instead of the legacy duplicate async decrement loops, but production rollout still requires duplicate audits, transaction policy, shop transaction policy, and DM failure policy | Gacha/Economy Feature Owners | open |
| Legacy gacha prize-list embed can exceed Discord field limit | Feature parity/Bug fix | `giftlist.js` adds one embed field per prize and does not guard the 25-field Discord limit | Guilds with more than 25 prizes can receive a failed interaction | Medium | Go preserves the exact single-embed layout for up to 25 prizes and splits larger pools into multiple embeds; tests cover the overflow path | Gacha Feature Owner | mitigated |
| Lottery disabled command synced before runtime gate is paired | Feature parity/Ops | `/抽獎設置` now has a managed command definition for the legacy unavailable response while runtime remains disabled by default | Users could see a slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_LOTTERY_DISABLED_COMMAND_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_LOTTERY_DISABLED_COMMAND=true`; staging preflight and scripts reject unpaired flags | Lottery Feature Owner | mitigated |
| Lottery creation mistaken as implemented | Feature parity/Data | Legacy `lotter_create.js` returns unavailable before the creation logic, but the option-rich command still exists | Operators could assume lottery creation/buttons are ready and expose non-working or unsafe old behavior | Medium | Current Go slice returns only the legacy unavailable embed, performs no `lotters` write, and does not register `lotter*` component handlers; creation requires a later repository/idempotency/design slice | Lottery Feature Owner | open |
| Stats query command synced before runtime gate is paired | Feature parity/Ops | `/統計系統查詢` now has a managed command definition while runtime remains disabled by default | Users could see a slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_STATS_QUERY_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_STATS_QUERY=true`; staging preflight and scripts reject unpaired flags | Stats Feature Owner | mitigated |
| Stats create command synced before runtime gate is paired | Feature parity/Ops | `/統計系統創建` now has a managed command definition and creates Discord channels while runtime remains disabled by default | Users could see a slash command that cannot route, or operators could expose channel-creating behavior without staging review | Medium | Runtime requires `MHCAT_FEATURE_STATS_CREATE_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_STATS_CREATE=true`; staging preflight and scripts reject unpaired flags; docs require isolated staging guild/database | Stats Feature Owner | mitigated |
| Stats role-count command synced before runtime gate is paired | Feature parity/Ops | `/統計身分組人數` now has a managed command definition and creates Discord channels while runtime remains disabled by default | Users could see a slash command that cannot route, or operators could expose channel-creating behavior without staging review | Medium | Runtime requires `MHCAT_FEATURE_STATS_ROLE_COUNT_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_STATS_ROLE_COUNT=true`; staging preflight and scripts reject unpaired flags; docs require isolated staging guild/database | Stats Feature Owner | mitigated |
| Stats delete command synced before runtime gate is paired | Feature parity/Ops | `/統計系統刪除` now has a managed command definition while runtime remains disabled by default | Users could see a slash command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_STATS_DELETE_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_STATS_DELETE=true`; staging preflight and scripts reject unpaired flags | Stats Feature Owner | mitigated |
| Stats rename worker enabled without scheduler ownership review | Feature parity/Discord rate limits | `channel_status.js` parity now has a disabled-by-default Go worker that renames `numbers` and `role_numbers` channels every 20 minutes when explicitly enabled | Running the Go worker beside the legacy worker, across multiple Go processes, or against production configs without review could duplicate channel mutation attempts and increase Discord rate-limit exposure | Medium | `MHCAT_FEATURE_STATS_RENAME_WORKER_ENABLED=false` by default; requires Gateway and Guild Members intent; docs require isolated staging/disposable config rows and avoiding parallel legacy ownership; missing channels and Discord/API failures are skipped/logged | Stats Feature Owner | open |
| Birthday config command synced before runtime gate is paired | Feature parity/Ops | `/生日系統` now has a managed command definition for the birthday config slice while runtime remains disabled by default | Users could see birthday subcommands that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_BIRTHDAY_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_BIRTHDAY_CONFIG=true`; staging preflight and scripts reject unpaired flags | Birthday Feature Owner | mitigated |
| Birthday profile/date slice mistaken as full birthday implementation | Feature parity/Scheduler | Current Go slices write `/生日系統 祝福語設定`, `增加`, `是否允許管理員設定`, `刪除`, and `生日列表`, while the sender/role scheduler in `handler/gift.js` is commented out | Operators could assume birthday notifications or role assignment are active after syncing the command | Medium | Document command-interaction scope; keep scheduler and role delivery disabled until a separate ADR/reviewed slice | Birthday Feature Owner | open |
| Birthday add pending state lost on process restart | Reliability/UX | Go intentionally uses versioned custom IDs plus process-local state for the two-step birthday add flow because legacy `hour_menu`/`min_menu` IDs are generic and ambiguous | A user who clicks an old select after timeout or restart must rerun `/生日系統 增加`; existing birthday data remains unchanged | Low | Five-minute state lifetime mirrors the legacy collector; stale/cross-user clicks return safe ephemeral retry/error responses; existing rows are only replaced after minute selection completes | Birthday Feature Owner | mitigated |
| Birthday allow-admin legacy save bug intentionally fixed | Feature parity/Bug fix | Legacy `/生日系統 是否允許管理員設定` constructs the replacement `birthday` document but never saves it, despite returning a success embed | Preserving the bug would make the Go command visibly succeed while losing the user's preference | Medium | Go persists `birthdays.allow`, preserves existing date/time fields, writes null date/time placeholders for new profiles, and documents/tests the intentional change | Birthday Feature Owner | mitigated |
| Birthday config writes before duplicate singleton audit | MongoDB/Data | Legacy `birthday_set` has singleton `{guild}` semantics without a confirmed unique index, and legacy delete+insert could leave duplicate rows after failures | Future birthday date/profile/scheduler code may read an arbitrary duplicate config; unique index creation would fail before cleanup | High | Go updates all duplicate `{guild}` rows and creates no index; production rollout still requires duplicate audit before unique index or scheduler enablement | Birthday Feature Owner | open |
| Announcement config command synced before runtime gate is paired | Feature parity/Ops | `公告頻道設置` now has a managed command definition while runtime remains disabled by default | Users could see a command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_ANNOUNCEMENT_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_ANNOUNCEMENT_CONFIG=true`; staging preflight and scripts reject unpaired flags | Announcement Feature Owner | mitigated |
| Bound announcement relay enabled without staging review | Feature parity/Privacy | Go now implements the legacy `ann_message.js` relay behind `MHCAT_FEATURE_ANNOUNCEMENT_RELAY_ENABLED=true`, which requires Message Content and deletes user messages after sending the bot relay | Operators could enable privileged content handling or message deletion in the wrong guild without validating permissions, mention behavior, and attachment handling | High | Relay remains disabled by default; config validation requires gateway, Guild Messages, and Message Content flags; staging preflight allows Message Content only for this relay; Go suppresses `tag` mentions and sends before deleting; run staging smoke before production enablement | Announcement Feature Owner | open |
| Announcement send tag mentions intentionally suppressed | Feature parity/Security | Legacy `/公告發送` preview and final send used user-entered `tag` content without `allowedMentions`, allowing pre-confirmation and final pings | User-visible pings do not fire exactly as legacy, but accidental or abusive `@everyone`/role/user pings are prevented | Medium | Document as intentional safety fix; future operator-controlled mention allowlist can be added only with ADR, tests, and explicit config | Announcement Feature Owner | mitigated |
| Text-XP config commands synced before runtime gate is paired | Feature parity/Ops | `聊天經驗設定` and `聊天經驗刪除` have managed command definitions while runtime remains disabled by default | Users could see commands that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_TEXT_XP_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_TEXT_XP_CONFIG=true`; staging preflight and scripts reject unpaired flags | XP Feature Owner | mitigated |
| Text-XP config writes before duplicate singleton audit | MongoDB/Data | Legacy `text_xp_channel` has no confirmed unique index and delete+insert could leave duplicate guild rows | Go level-up announcements read one legacy config row, so duplicate configs could choose an unexpected channel/message; unique index would fail without cleanup | High | Go config repository updates/deletes all duplicate `{guild}` rows and creates no index; production rollout still requires duplicate audit before unique index or broad XP announcer enablement | XP Feature Owner | open |
| Full Text-XP behavior mistaken as enabled by config/accrual slices | Feature parity/Privacy | `聊天經驗設定` configures announcement channel/message/color, and `MHCAT_FEATURE_TEXT_XP_ACCRUAL_ENABLED` updates `text_xps`, sends configured level-up announcements/fallbacks, applies configured text reward roles, and grants legacy XP coin rewards after a usable announcement path; rank/profile rendering remains separate | Operators may think full text XP is active after syncing config commands or enabling accrual, while profile/rank card behavior is still absent | Medium | Document config/accrual/announcement/reward-role/coin scope; require gateway, Guild Messages, and Message Content for accrual; implement profile cards in a separate reviewed slice | XP Feature Owner | open |
| Voice-XP config commands synced before runtime gate is paired | Feature parity/Ops | `語音經驗設定` and `語音經驗刪除` have managed command definitions while runtime remains disabled by default | Users could see commands that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_VOICE_XP_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_VOICE_XP_CONFIG=true`; staging preflight and scripts reject unpaired flags | XP Feature Owner | mitigated |
| XP reward-role config commands synced before runtime gate is paired | Feature parity/Ops | `聊天經驗身分組設定` and `語音經驗身分組設定` have managed command definitions while runtime remains disabled by default | Users could see reward-role config commands that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_XP_ROLE_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_XP_ROLE_CONFIG=true`; staging preflight and scripts reject unpaired flags | XP Feature Owner | mitigated |
| XP reward-role config writes before duplicate audit | MongoDB/Data | Legacy `chat_role` and `voice_role` rows use `{guild, leavel, role}` identity without confirmed unique indexes, and `leavel` is stored as a string with the legacy misspelling | Text XP role assignment may process duplicate or mixed-type rows unexpectedly, and unique indexes could fail without cleanup | High | Go preserves `leavel`, deletes duplicate exact matches before insert, deletes duplicate exact matches on remove, and creates no index; production rollout still requires duplicate/type audit before broad automatic role assignment | XP Feature Owner | open |
| XP reward-role config mistaken as automatic role assignment | Feature parity/Product | `聊天經驗身分組設定` and `語音經驗身分組設定` manage `chat_roles`/`voice_roles` config rows; text role grant/removal now runs only through the separate text accrual gate, while voice role assignment remains disabled | Operators may assume level roles are awarded or removed after syncing config commands alone, or may assume voice roles are active because text roles are active | Medium | Document command-only scope; require the text accrual event gate for text role runtime; keep voice XP accrual and voice automatic role assignment disabled until separate reviewed slices | XP Feature Owner | open |
| XP profile disabled commands synced before runtime gate is paired | Feature parity/Ops | `聊天經驗` and `語音經驗` now have managed command definitions for the legacy replacement response while runtime remains disabled by default | Users could see commands that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_XP_PROFILE_DISABLED_COMMANDS_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_XP_PROFILE_DISABLED_COMMANDS=true`; staging preflight and scripts reject unpaired flags | XP Feature Owner | mitigated |
| Disabled XP profile responses mistaken as rank/profile implementation | Feature parity/Product | Legacy `聊天經驗` and `語音經驗` currently return a removal/replacement embed before any XP lookup | Operators may assume XP profile cards or rank lookups are active because the slash commands are visible | Medium | Document disabled-response-only scope; keep XP collection reads, rank rendering, accrual, and intents disabled until separate reviewed slices | XP Feature Owner | open |
| XP admin command synced before runtime gate is paired | Feature parity/Ops | `/經驗值改變` now has a managed command definition while runtime remains disabled by default | Users could see an XP write command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_XP_ADMIN_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_XP_ADMIN=true`; staging preflight and scripts reject unpaired flags | XP Feature Owner | mitigated |
| XP admin writes before full XP ownership review | Data correctness/Ops | `/經驗值改變` mutates `text_xps` and `voice_xps` directly while XP accrual, rank, reward-role assignment, duplicate audits, and Node/Go ownership are still partial | Staging or production operators could change real XP rows before rollback, duplicate-row, or automatic reward behavior is reviewed | High | Disabled by default; require isolated staging `text_xps`/`voice_xps` fixtures; preserve legacy `xp`/`leavel` strings and `voice_xps.leavejoin=leave`; do not production-sync until XP ownership, duplicate audit, and reward/rank rollout are approved | XP Feature Owner | open |
| XP reset command synced before runtime gate is paired | Feature parity/Ops | `/經驗值重製` now has a managed command definition while runtime remains disabled by default | Users could see an owner-only destructive command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_XP_RESET_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_XP_RESET=true`; staging preflight and scripts reject unpaired flags | XP Feature Owner | mitigated |
| XP reset enabled without message-content readiness | Feature parity/Data | Full-server XP reset preserves the legacy `^確認^` message collector and therefore needs gateway, Guild Messages, and Message Content intents | Operators could expose a command that arms a confirmation prompt but never receives the confirming message, or enable privileged message-content access without review | High | Config validation and staging preflight require `MHCAT_DISCORD_ENABLE_GATEWAY=true`, `MHCAT_DISCORD_GUILD_MESSAGES_INTENT=true`, and `MHCAT_DISCORD_MESSAGE_CONTENT_INTENT=true`; docs require isolated staging fixtures | XP Feature Owner | mitigated |
| XP reset deletes production XP rows before ownership review | Data correctness/Ops | `/經驗值重製` can delete individual or all guild `text_xps`/`voice_xps` rows while Node/Go XP ownership and duplicate audits remain partial | Accidental production enablement could permanently erase user XP data | High | Disabled by default; requires guild owner check plus legacy confirmation for full reset; command sync and runtime gates remain staging-only until XP ownership, backups, and duplicate audits are approved | XP Feature Owner | open |
| XP rank commands synced before runtime gate is paired | Feature parity/Ops | `/聊天排行榜` and `/語音排行榜` now have managed command definitions while runtime remains disabled by default | Users could see read-only leaderboard commands that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_XP_RANK_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_XP_RANK=true`; staging preflight and scripts reject unpaired flags | XP Feature Owner | mitigated |
| XP rank mistaken as XP accrual/profile implementation | Feature parity/Product | `/聊天排行榜` and `/語音排行榜` read existing XP rows and render leaderboard PNGs only | Operators may assume XP accrual, `/聊天經驗` profile cards, or automatic reward roles are active after syncing leaderboard commands | Medium | Document read-only scope; keep XP accrual, profile cards, reward roles, gateway intents, and writes disabled until separate reviewed slices | XP Feature Owner | open |
| Voice-room config commands synced before runtime gate is paired | Feature parity/Ops | `語音包廂設置` and `語音包廂刪除` have managed command definitions while runtime remains disabled by default | Users could see commands that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_VOICE_ROOM_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_VOICE_ROOM_CONFIG=true`; staging preflight and scripts reject unpaired flags | Voice Feature Owner | mitigated |
| Voice-room lock command synced before runtime and Voice State readiness are paired | Feature parity/Ops | `/上鎖頻道` has a managed command definition while runtime remains disabled by default and depends on Discord voice state cache | Users could see a command that cannot know their current voice channel if command sync, runtime, gateway, and Voice State flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_VOICE_ROOM_LOCK_ENABLED=true`, gateway, and Voice State intent; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_VOICE_ROOM_LOCK=true`; staging preflight and scripts reject unpaired sync/runtime flags and preflight checks readiness | Voice Feature Owner | mitigated |
| Voice-room config mistaken as full voice subsystem parity | Feature parity/Discord voice | Current slices write/delete `voice_channels`, create/move/delete dynamic rooms from trigger joins, write `voice_channel_ids`, seed/replace `lock_channels`, and enforce passworded lock rows; voice XP and voice logging are still separate paths | Operators may assume enabling voice-room config also enables voice XP/logging or that `/語音包廂刪除` deletes already-active dynamic rooms | Medium | Document the dynamic-room scope, require gateway Voice State intent for event behavior, and keep voice XP/logging disabled until separate reviewed slices | Voice Feature Owner | open |
| Join-role config commands synced before runtime gate is paired | Feature parity/Ops | `加入身份組設置` and `加入身份組刪除` have managed command definitions while runtime remains disabled by default | Users could see commands that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_JOIN_ROLE_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_JOIN_ROLE_CONFIG=true`; staging preflight and scripts reject unpaired flags | Onboarding Feature Owner | mitigated |
| Welcome-message config commands synced before runtime gate is paired | Feature parity/Ops | `加入訊息設置` and `退出訊息設置` have managed command definitions while runtime remains disabled by default | Users could see commands that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_WELCOME_MESSAGE_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_WELCOME_MESSAGE_CONFIG=true`; staging preflight and scripts reject unpaired flags | Onboarding Feature Owner | mitigated |
| Verification config command synced before runtime gate is paired | Feature parity/Ops | `驗證設置` now has a managed command definition while runtime remains disabled by default | Users could see a command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_VERIFICATION_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_VERIFICATION_CONFIG=true`; staging preflight and scripts reject unpaired flags | Onboarding Feature Owner | mitigated |
| Verification config mistaken as full verification implementation | Feature parity/Discord intents | `/驗證設置` only writes the legacy `verifications` config row and is separate from the gated `/驗證` runtime flow | Operators could sync setup and assume the member verification workflow is active | Medium | Document config-only scope; require separate `MHCAT_FEATURE_VERIFICATION_FLOW_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_VERIFICATION_FLOW` flags for `/驗證` | Onboarding Feature Owner | mitigated |
| Verification flow command synced before runtime gate is paired | Feature parity/Ops | `/驗證` now has a managed command definition while runtime remains disabled by default | Users could see a command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_VERIFICATION_FLOW_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_VERIFICATION_FLOW=true`; staging preflight and scripts reject unpaired flags | Onboarding Feature Owner | mitigated |
| Verification custom ID security fix changes internals | Feature parity/Security | Legacy `<captcha>verification` and `<captcha>ver` embed the captcha answer in the component/modal IDs; Go-generated IDs use a bounded state ID instead | Existing live legacy messages still need compatibility, while new messages should not leak answers in custom IDs | Medium | Support legacy IDs for compatibility; generate `mhcat:v1:verification:*:state=<id>` for new messages; document as intentional safety fix and test that new IDs do not contain the answer | Onboarding Feature Owner | mitigated |
| Verification challenge state is process-local | Reliability/Rollout | Go `/驗證` stores captcha answers in an in-memory map with a 5-minute TTL | Restart, process switch, or a different shard/process handling the modal can make a valid answer fail | Medium in single-process staging; High in multi-process rollout | Keep production disabled by default; stage in a single process first; add a bounded shared state store before multi-process/sharded production rollout | Onboarding Feature Owner | open |
| Verification flow side effects enabled without staging review | Feature parity/Discord permissions | Go `/驗證` can add roles and optionally change nicknames when explicitly enabled | Wrong role, hierarchy, or nickname-template rollout could mutate staging/production members unexpectedly | High | Disabled by default; require role below bot hierarchy, staging command sync/runtime pairing, and manual smoke for captcha, wrong answer, role add, nickname, and owner nickname paths before production | Onboarding Feature Owner | open |
| Account-age command synced before runtime gate is paired | Feature parity/Ops | `/帳號需創建時數` now has a managed command definition while runtime remains disabled by default | Users could see a command that cannot route if command sync and runtime flags are not paired | Medium | Runtime requires `MHCAT_FEATURE_ACCOUNT_AGE_CONFIG_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_ACCOUNT_AGE_CONFIG=true`; staging preflight rejects unpaired flags | Onboarding Feature Owner | mitigated |
| Account-age member gate enabled without intent/gateway readiness | Safety/Discord intents | The gate can DM, kick, and log on `guildMemberAdd` when `create_hours` requires older accounts | Misconfigured rollout could fail silently or kick unexpectedly | High | Disabled by default; `MHCAT_FEATURE_ACCOUNT_AGE_POLICY_ENABLED=true` requires `MHCAT_DISCORD_ENABLE_GATEWAY=true` and `MHCAT_DISCORD_GUILD_MEMBERS_INTENT=true`; staging smoke must use isolated guild/database | Onboarding Feature Owner | open |
| Account-age gate ordering regression | Feature parity/Event routing | Legacy `welcome.js` checks `create_hours` before join-role/welcome logic | Too-new accounts could receive join roles or welcome messages after a kick | High | Event dispatcher now supports `ErrStopPropagation`; account-age policy registers before join-role assignment and tests assert later handlers do not run after a matched kick | Onboarding Feature Owner | mitigated |
| Duplicate `create_hours` rows | Mongo compatibility | Live catalog notes singleton `{guild}` semantics without unique index; no unique index is applied by default | `findOne` compatibility may read an arbitrary duplicate and config delete/update behavior may surprise operators | Medium | Preserve collection/fields; command delete uses `DeleteMany` as an intentional cleanup fix; unique index remains audit-gated and requires duplicate audit before apply | Mongo Feature Owner | open |
| Legacy leave modal first-field route mismatch | Component routing | Legacy `leave_message.js` creates `leave_msgcolor` as the first modal field, while the initial Go parser accepted only `leave_msgcontent` for `welcome/leave_submit` | Live leave-message modal submits could fail to route | High | Parser now accepts both `leave_msgcolor` and `leave_msgcontent`; golden fixtures cover actual color-first legacy order | Onboarding Feature Owner | mitigated |
| Join-role assignment enabled without staging review | Feature parity/Discord intents | Go now implements member-add role assignment from `join_roles` behind `MHCAT_FEATURE_JOIN_ROLE_ASSIGNMENT_ENABLED=true`; it requires Guild Members intent and role mutation permissions | Operators could assign roles in the wrong guild or expose an unreviewed privileged-intent path | High | Disabled by default; config validation and staging preflight require gateway plus Guild Members intent; command config and event assignment gates are separate; test in staging with roles below the bot before production | Onboarding Feature Owner | open |
| Welcome-message delivery enabled without staging review | Feature parity/Discord intents | Go now implements member-add welcome delivery from dashboard/legacy `join_messages` behind `MHCAT_FEATURE_WELCOME_MESSAGE_DELIVERY_ENABLED=true`; it requires Guild Members intent and channel send permissions | Operators could send legacy welcome embeds to the wrong channel or allow unintended user mentions from old templates | Medium | Disabled by default; config validation and staging preflight require gateway plus Guild Members intent; delivery performs no Mongo writes, allows only the joining user mention for tag placeholders, and requires isolated staging smoke before production | Onboarding Feature Owner | open |
| Leave-message delivery enabled without staging review | Feature parity/Discord intents | Go now implements member-remove leave-message delivery from `leave_messages` behind `MHCAT_FEATURE_LEAVE_MESSAGE_DELIVERY_ENABLED=true`; it requires Guild Members intent and channel send permissions | Operators could send leave embeds to the wrong channel or expose an unreviewed privileged-intent path | Medium | Disabled by default; config validation and staging preflight require gateway plus Guild Members intent; test `/退出訊息設置` and member removal in an isolated guild before production | Onboarding Feature Owner | open |
| Voice-XP config writes before duplicate singleton audit | MongoDB/Data | Legacy `voice_xp_channel` has no confirmed unique index and delete+insert could leave duplicate guild rows | Future voice XP announcement code may read an arbitrary duplicate config; unique index would fail without cleanup | High | Go config repository updates/deletes all duplicate `{guild}` rows and creates no index; production rollout still requires duplicate audit before unique index or voice XP announcer enablement | XP Feature Owner | open |
| Voice-XP accrual/rank mistaken as enabled by config slices | Feature parity/Privacy | `語音經驗設定` only configures announcement channel/message/color; voice XP accrual runs only when the separate event gate, gateway, and Voice State intent are explicitly enabled | Operators may think voice XP is active after syncing config commands alone, or may enable voice XP runtime before staging data review | Medium | Document config-only/runtime separation; keep Voice State intent disabled by default unless the session gate is intentionally tested against disposable staging rows; rank/profile-card rollout remains separate | XP Feature Owner | open |
| Text-XP preview mention behavior intentionally safer than legacy | Feature parity/Security | Legacy `text_set.js` sends preview content with default mention parsing; Go sends the same visible text with empty allowed mentions | Preview pings do not fire exactly as legacy, but accidental `@everyone`/role/user pings are prevented | Low | Document as intentional safety fix; level-up announcements preserve the leveling-user ping with explicit allowed mentions only | XP Feature Owner | mitigated |
| Voice XP interval leaks/restart drift | Reliability/Data | Legacy used per-user intervals and had a `data.save` typo; Go now persists `leavejoin` session state, starts bounded workers from live joins, reconciles existing joined rows on startup, and stops workers on leave/shutdown | Duplicate or missed XP is still possible if Node and Go both own voice XP rows, or if staging enables the event gate without isolated data | Medium | Runtime remains disabled by default; test only one owner against disposable staging `voice_xps` rows, keep shutdown hooks covered, and complete rollout ownership review before production | Performance Agent | open |
| Inactive/commented features ambiguous | Product/Parity | birthday scheduler commented; lottery creation disabled | Wrongly restoring or removing behavior | Medium | ADR per feature; preserve inactive behavior unless approved | Behavior Agent | open |
| Dashboard/external worker unknown | Integration | README/dashboard links and `chatgpt` handoff docs absent | Data contract breakage | Medium | Audit dashboard/worker before changing shared collections | Rollout Agent | open |
| Schema changes can break rollback | Data/Rollout | user permits changes, but Node rollback required | Node cannot read Go writes | Medium | dual-read/write, ADR, dry-run backfill, rollback tests | MongoDB Data Agent | open |
| Index apply misuse | MongoDB/Ops | Wave 3 adds guarded index creation code in `mhcat-mongo-index` | Unique/TTL/large index builds could affect production if run carelessly | Medium | dry-run default; no startup index creation; no index deletion; `--apply` required; unique requires `--allow-unique` and clean duplicate audit; TTL requires `--allow-ttl` and retention ADR | MongoDB Data Agent | open |
| Field-level Mongo schema coverage remains incomplete | MongoDB/Data | Platform Wave B catalog covers all 47 model files but still marks entries incomplete for strict BSON field/type coverage | Repositories could decode mixed/missing fields incorrectly if feature waves skip fixtures | Medium | Add BSON fixture decode tests and mixed-type compatibility tests per feature group before writes | MongoDB Data Agent | open |
| Utility usage count parity gap | Feature parity/Data | Wave 5.1 adds `UsageTracker` but production implementation is no-op; legacy increments `all_use_count` in slash dispatch | Command usage statistics will not match legacy until Mongo repository strategy is approved | Medium | Keep no-op default; add fake tests; implement atomic `all_use_count` repository only after Mongo audit/catalog approval | Feature Pipeline Owner | open |
| Help UI placeholder parity regression | Feature parity/Discord | Manual staging `/help` showed the Wave 5.1 plain-text placeholder instead of the legacy embed/select menu | Users see a rebuilt interface instead of the legacy MHCAT help UI | Medium | Wave 5.8 restores legacy help overview, command detail, category select, and link-button UI; keep regression tests on embed/component shape | Feature Pipeline Owner | resolved |
| Info bot UI placeholder parity regression | Feature parity/Discord | Wave 5.1 `/info bot` returned plain text status instead of the legacy system embed and refresh button | Users see a rebuilt interface instead of the legacy MHCAT bot-info UI | Medium | Wave 5.9 restores legacy bot-info embed fields, timestamp, refresh button, and `botinfoupdate` component route; keep regression tests on embed/component shape | Feature Pipeline Owner | resolved |
| Info user/guild unsupported parity gap | Feature parity/Discord | Legacy `/info` includes `user` and `guild` subcommands; previous Go runtime returned unsupported text | Users could not use the full legacy `/info` utility surface | Medium | Wave 5.11 restores read-only legacy-style `/info user` and `/info guild` embeds with safe lookup errors; command sync is still manual through CLI | Feature Pipeline Owner | resolved |
| Real gateway smoke not yet verified | Discord/Rollout | Wave 5.2 wires `InteractionCreate` runtime path but no live Discord smoke test was run in this environment | First staging gateway run may reveal DiscordGo event/session edge cases | Medium | Keep gateway and smoke mode opt-in; run staging-token smoke test before canary; never register commands from bot startup | Runtime Owner | open |
| Staging command apply misuse | Discord/Ops | Wave 5.3 adds a staging guild apply path for managed utility commands | Wrong env could mutate non-staging guild command definitions | Medium | Require `MHCAT_STAGING_MODE`, explicit apply allow, guild scope, expected command ownership, no delete, no bulk overwrite, and optional application ID pin | Runtime Owner | open |
| Staging env readiness unknown | Discord/Ops | Wave 5.4 could not run real smoke because staging env was absent | Operators may attempt smoke/apply with incomplete env | Medium | Run `mhcat-staging-preflight` before staging scripts; report missing env without printing secrets | Runtime Owner | open |
| Side-effect ports used before policy/queueing | Discord/Reliability/Safety | Platform Wave A adds driver-agnostic message/channel/role/member/audit-log ports for future features, but no feature-specific permission, hierarchy, or rate-limit queue is implemented yet | Future ticket/onboarding/moderation/logging waves could create side effects too quickly or without complete policy checks | Medium | Treat ports as adapter shells only; require per-feature permission tests, rate-limit strategy, and safe allowed-mentions before wiring handlers | Runtime Owner | open |
| Ticket setup writes config before modal validation | Feature parity/Data | Legacy `/私人頻道設置` saves `tickets` config before `ticketcolor`/`tickettitle`/`ticketcontent` modal values are submitted and validated | Invalid/abandoned setup can leave active ticket config without a matching panel | Medium | Go ticket setup modal now persists config only after valid modal submit; runtime default still requires explicit repository wiring before production use | Ticket Feature Owner | mitigated |
| Legacy ticket `nal` modal route missing | Feature parity/Discord | Wave 4 parser recognized `nal` + `ticketcolor` as `ticket/panel_submit`, but the ticket module did not register that route | Live legacy setup modals could parse but fail routing after Go runtime rollout | Medium | Ticket module now registers the legacy modal route and tests assert channel panel send plus success edit | Ticket Feature Owner | resolved |
| Ticket open/close side effects not implemented yet | Feature parity/Discord | Earlier Go ticket slice sent the legacy `tic` panel button but did not create/delete private ticket channels | Users cannot complete the full private-channel workflow if ticket commands are synced before side-effect handlers land | High | `tic` open and `del` close handlers, permission overwrites, channel deletion checks, and fake side-effect tests are implemented; keep commands unsynced until explicit runtime/staging wiring is enabled | Ticket Feature Owner | mitigated |
| Ticket commands synced before runtime side effects are enabled | Feature parity/Ops | Ticket command definitions exist and can now be included in command sync, while runtime side effects are disabled by default | Users could see ticket commands that cannot complete if command sync and runtime flags are not enabled together | High | Runtime requires `MHCAT_FEATURE_TICKETS_ENABLED=true`; command sync requires staging guild plus `MHCAT_COMMAND_SYNC_INCLUDE_TICKETS=true`; run staging smoke before production sync | Ticket Feature Owner | mitigated |
| Ticket bot permission overwrite may be absent in default runtime | Feature parity/Discord | Legacy channel create includes an explicit bot member permission overwrite; default runtime cannot reliably know the bot user ID before gateway ready | In guilds without administrator/manage-channel visibility, bot access to ticket channels may depend on role permissions | Medium | Staging smoke must verify bot can send/delete in ticket channels; add bot identity provider if required | Ticket Feature Owner | open |
