# Feature Parity Checklist

Canonical update: read-only `/代幣查詢`, `/代幣排行榜`, `my-profile`, the `/簽到` plus `/簽到列表` family, `/coin-related-settings`, `/代幣增加`, `/剪刀石頭布`, `/代幣重製`, `/代幣遊戲`, `/代幣商店`, and `打工系統` are parity-audited in [95-economy-query.md](95-economy-query.md) through [105-work-system.md](105-work-system.md), including definitions, lifecycle, UI, scalar coercion, components/events, usage, Mongo duplicate behavior, workers, staging smoke, and rollback. The broader economy domain remains in progress.

Status: Ticket config repository foundation, ticket setup/delete UI handlers, `tic`/`del` open/close side-effect handlers, runtime feature flag, and staging command-sync include gate are implemented after Platform Wave B. A repeatable slash-command UI parity audit now compares all 74 legacy slash command definitions against current Go definitions; the current report shows 74 matching Go definitions, zero UI metadata drift, and no legacy command names without Go definitions. Poll Wave A/B now implements gated poll create/vote/owner-menu/result chart/text export/Excel export handlers with legacy-style visible UI and safer repository vote semantics. Built-in `help`, `ping`, `info bot`, `info shard`, `info user`, and `info guild` UI, lifecycle, metadata, metrics, usage ownership, and no-Mongo behavior are parity-audited in [94-utility-builtins.md](94-utility-builtins.md); `/info shard` preserves the empty initial legacy embed. Read-only `/代幣查詢`, read-only `/代幣排行榜`, and read-only `my-profile` are implemented behind explicit economy feature flags. `/簽到` has a disabled-by-default, staging-gated first write slice with legacy-style loading/image/buttons, and `/簽到列表` is implemented as a read-only list/export path under the same gate. `/代幣增加`, `/剪刀石頭布`, and `/代幣重製` are implemented as disabled-by-default, staging-gated coin write slices; `/代幣增加` preserves admin adjustment guards, `/剪刀石頭布` preserves legacy win/loss/tie wager behavior, and `/代幣重製` preserves the owner-only `^確認^` destructive reset flow. `/代幣遊戲` is implemented behind explicit economy game runtime and command-sync gates with legacy two-player wager game UI, and `/代幣商店` is implemented behind explicit economy shop runtime and command-sync gates with legacy shop setup/query/purchase UI; Role-selection slash commands, legacy `nal` role-button modal/button routing, and reaction add/remove role events are implemented behind explicit runtime, command-sync, gateway, and Guild Message Reactions intent gates. The economy daily reset now has a lease-gated one-shot apply path and a separately gated recurring worker, while work payout now has crash-idempotent one-shot and separately gated recurring paths. `打工系統` now has a gated command-definition shell, exact legacy dashboard redirect for `新增打工事項`, legacy-style `打工介面` list/captcha/detail/start UI with an explicit atomic `work_users` start-write path, and admin setup/delete/energy writes behind explicit admin repository wiring. `/警告紀錄`, `/警告設定`, `/警告清除`, `/警告全部清除`, and `/警告` are implemented behind separate warning runtime and staging command-sync gates; `/警告` appends `warndbs`, sends legacy-style DMs, and can run configured kick/ban threshold actions from `errors_sets`. `/刪除訊息` is implemented behind explicit message-cleanup runtime and staging command-sync gates, deletes recent Discord messages only, and writes no Mongo data. `/刪除資料` is implemented behind explicit delete-data runtime and staging command-sync gates, preserves the legacy destructive select UI, and deletes selected guild-scoped legacy config rows. `/翻譯` is implemented behind explicit translate runtime and staging command-sync gates. Read-only `/查看餘額` and write `/兌換` are implemented behind separate explicit runtime and staging command-sync gates. `/自動聊天頻道` and `/自動聊天頻道刪除` are implemented behind explicit config/runtime gates; separately gated local and paid MessageCreate runtimes restore the legacy balance split and Mongo worker handoff. `automatic-notification`, `/自動通知列表`, and `/自動通知刪除` are implemented as auto-notification config-maintenance commands behind explicit runtime and staging command-sync gates; setup creates pending `cron_sets` rows, completes direct cron modal submits and owner-scoped simplified weekday/hour/minute selections, writes rollback-compatible message payloads, and sends setup previews; a separately gated lease-backed recurring delivery worker restores scheduled sends. `/防詐騙網址` is implemented as a config-only `good_webs` toggle behind explicit runtime and staging command-sync gates; `/詐騙網址回報` is implemented behind explicit runtime, command-sync, and webhook gates; message deletion is implemented behind a separate gateway runtime gate. `/語音包廂設置` and `/語音包廂刪除` are implemented as `voice_channels` maintenance commands, dynamic trigger joins now create/move/delete tracked voice rooms with `voice_channel_ids` and owner/category permission overwrites when gateway Voice State events are enabled, and `/上鎖頻道` plus existing passworded `lock_channels` join enforcement are implemented behind explicit runtime and staging command-sync/gateway gates. `/set-log-channel` now has a gated logging-config slice with legacy-style visible UI and rollback-compatible `loggings` writes. `/扭蛋`, `/扭蛋獎池查詢`, `/扭蛋獎池增加`, `/扭蛋獎品編輯`, and `/扭蛋獎池刪除` are implemented behind explicit gacha runtime and staging command-sync gates; `/扭蛋` mutates staging `coins`/`gifts`, can send prize-code DMs and notification-channel messages, and remains disabled by default; shop writes are separately gated. `/抽獎設置` preserves the legacy disabled-command unavailable response behind explicit runtime and command-sync gates; existing lottery-message enter/search/reroll/stop buttons are independently implemented behind a component runtime gate, while creation remains disabled. `/統計系統查詢` now preserves the legacy static stats help embed, `/統計系統創建` creates legacy stats channels and writes `numbers`, `/統計身分組人數` creates role-count channels and writes `role_numbers`, `/統計系統刪除` deletes legacy `numbers` config rows behind explicit runtime and command-sync gates, and the event-only stats rename worker updates `numbers`/`role_numbers` stat channels behind `MHCAT_FEATURE_STATS_RENAME_WORKER_ENABLED`; all stats mutation paths remain disabled by default. `/聊天經驗` and `/語音經驗` now preserve the legacy disabled-command replacement response behind explicit runtime and command-sync gates; XP profile card rendering remains disabled. Config-only `聊天經驗設定`, `聊天經驗刪除`, `語音經驗設定`, `語音經驗刪除`, text XP accrual profile writes, configured level-up announcements, text reward-role changes, and XP coin rewards, voice XP session tracking, voice XP 30-second runtime loop, voice XP restart reconciliation, `聊天經驗身分組設定`, `語音經驗身分組設定`, gated `/經驗值改變` XP admin writes, `/經驗值重製` XP deletes, read-only `/聊天排行榜` and `/語音排行榜` XP leaderboard PNGs, `/驗證設置`, gated `/驗證` captcha/role/nickname flow, `/帳號需創建時數`, `/生日系統 祝福語設定`, `/生日系統 增加`, `/生日系統 是否允許管理員設定`, `/生日系統 刪除`, `/生日系統 生日列表`, and the gated member-add account-age kick/log flow are implemented behind explicit runtime and staging command-sync gates; XP profile cards and birthday notifications remain disabled. Ticket, poll, economy-query, economy coin-rank, economy RPS, economy game, economy shop, economy profile, economy sign-in, economy coin-admin, economy coin-reset, role-selection, work, warning-history, warning-settings, warning-removal, warning-issue, message-cleanup, delete-data, translate, balance-query, redeem, autochat-config, autochat-fallback, autochat-paid-handoff, auto-notification-config, auto-notification-delivery, daily-reset-scheduler, anti-scam-config, anti-scam-report, anti-scam-message-delete, voice-room config, voice-room lock, logging-config, gacha prize-list, gacha draw, gacha prize-create, gacha prize-edit, gacha prize-delete, lottery disabled-command, lottery components, stats-query, stats-create, stats-role-count, stats-delete, stats-rename-worker, birthday-config/profile, text-XP config, text-XP accrual, voice-XP config, voice-XP sessions, XP reward-role config, XP profile disabled-command, XP admin, XP reset, XP rank, verification config, verification flow, and account-age runtime side effects remain disabled by default and must be paired with staging-only command sync or event-only smoke gates before live use. The external ChatGPT worker remains an operator-confirmed dependency; birthday delivery and XP profile cards stay inactive because those legacy paths are commented out or unreachable.

Economy-game interaction parity now includes random-color and self-invites, full private tutorial embeds that remain available after rejection, knowledge/blackjack private acceptance feedback, higher/lower acceptance without an extra reply, the 500-millisecond first knowledge question, component-free five-second knowledge reveals with the countdown still running, the five-second higher/lower draw screen before settlement, and action-aware blackjack turn/card UI. Command-definition parity remains separate from this runtime behavior evidence.

| Feature | Legacy file | New Go package | Mongo collections | Discord APIs | Status | Tests | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Startup and lifecycle | `shard.js`, `index.js` | `internal/app`, `internal/adapters/discordgo` | none direct | Gateway/sharding | parity-audited | [107-runtime-lifecycle.md](107-runtime-lifecycle.md), lifecycle/failure-cleanup/ready/smoke/shutdown/signal/worker tests, race coverage | Mongo-first startup, explicit Gateway readiness, failure cleanup, idempotent graceful shutdown, and supervisor restart are covered; command/index mutation and hardcoded restart backdoors are intentionally removed; supported deployment remains one Discord session/shard pending explicit multi-session architecture and live smoke |
| Config/secrets | `.env.template`, `config.json`, `index.js` | `internal/config` | none | webhooks | parity-audited | [106-config-secrets.md](106-config-secrets.md), env alias/validation/redaction tests, staging no-secret tests, production-source AST scan | env-only credentials/deployment IDs, ignored untracked `.env`, blank tracked `.env.example`, controlled redaction, and hardcoded webhook/privileged-ID prevention are enforced; legacy credential rotation remains an external operator action |
| Command registration infrastructure | `handler/slash_commands.js` | `cmd/mhcat-command-sync`, `internal/discord/commands`, `internal/adapters/discordgo` | none | application commands REST | parity-audited | [108-command-registration-dispatch.md](108-command-registration-dispatch.md), 74/74 static audit, registry/diff/dry-run/apply/fake-client/staging/preflight tests | exact definitions are typed; bot startup performs no command writes; guarded CLI defaults dry-run and requires explicit apply/delete/bulk acknowledgements; staging remains guild-scoped and feature-paired |
| Slash dispatch | `events/SlashCommands.js` | `internal/discord/runtime`, `internal/discord/interactions`, `internal/adapters/mongo/repositories` | `all_use_counts` (gated) | interactions | parity-audited | [108-command-registration-dispatch.md](108-command-registration-dispatch.md), router/adapter/middleware/dispatcher/responder/wiring/repository/race tests | exact route dispatch, legacy unknown-command UI, feature-owned permission/response behavior, no effective cooldown, one optional atomic usage event per slash attempt, no component/modal usage, and controlled unresponded errors are covered |
| Shared Discord platform | `events/*.js`, Discord REST calls in feature files | `internal/discord/events`, `internal/core/ports`, `internal/adapters/discordgo` | none | messages/reactions/members/voice/channels/roles/audit logs | parity-audited | [109-discord-platform.md](109-discord-platform.md), intent/event/interaction/responder/side-effect/cache/audit-log/app/race tests | active features own typed ports for messages, DMs, reactions, members, roles, channels, voice, hierarchy, and audit logs; mentions suppress by default; privileged/event intents remain independently opt-in; live cache/rate-limit/hierarchy smoke and single-session scope remain rollout gates |
| Component/modal dispatch | `events/btn.js`, `modal.js`, `poll.js`, `rank.js` | `internal/discord/customid`, `internal/discord/interactions`, `internal/discord/responses` | many | buttons/selects/modals | parity-audited | [12-component-modal-grammar.md](12-component-modal-grammar.md), custom-ID golden/collision/fuzz tests, modal response tests, focused route tests, app wiring | active legacy creator/handler pairs have explicit compatibility parsers and gated routes; new IDs are versioned and bounded; dead `help-menus`, disabled rank controls, and ambiguous collector-local IDs are explicitly non-routable; live-message retirement/rollback remains feature-specific |
| Utility commands | `ping.js`, `info.js`, `help.js`, `translate.js` | `internal/core/services/utility`, `internal/discord/features/utility`, `internal/adapters/discordgo`, `internal/adapters/external` | optional global `all_use_counts` only | interactions/selects/buttons/host metrics/external translate HTTP | parity-audited for built-ins and translate | exact UI/lifecycle/metadata, Linux metrics, media URLs, usage ownership, registry, dry-run, runtime, staging guard, race, vet, and no-Mongo tests | `help`/`ping`/`info` are canonical in [94-utility-builtins.md](94-utility-builtins.md); `/翻譯` is canonical in [86-translate.md](86-translate.md); auto-chat is tracked separately |
| Auto chat | `chat.js`, `chat_delete.js`, `events/Chatbot.js`; balance/redeem commands `check_price.js`, `get_something.js` | `internal/core/services/autochat`, `internal/core/services/utility`, `internal/core/services/redeem`, `internal/adapters/mongo/repositories`, `internal/discord/features/autochat`, `internal/discord/features/balance`, `internal/discord/features/redeem` | `chats`, `chatgpts`, `chatgpt_gets`, `codes` | config interactions, balance query, redeem credit, local replies, and transactional paid worker handoff | parity-audited; all five independently gated surfaces remain disabled by default | domain/service/document/repository/real-Mongo/handler/UI/usage/app/config/preflight and replica-set transaction/race tests | Canonical contracts: [87-balance-query.md](87-balance-query.md), [88-redeem.md](88-redeem.md), [89-autochat-config.md](89-autochat-config.md), [90-autochat-fallback.md](90-autochat-fallback.md), and [91-autochat-paid.md](91-autochat-paid.md); production paid ownership still requires live external-worker smoke |
| Economy/sign/profile | `slashCommands/代幣系統/*` | `internal/core/services/economy`, `internal/adapters/mongo/repositories`, `internal/discord/features/economy` | `coins`, `gift_changes`, `sign_lists`, `work_*`, XP collections | interactions/buttons/DMs | parity-audited | [95-economy-query.md](95-economy-query.md) through [105-work-system.md](105-work-system.md), BSON decode tests, read-only query/rank/profile service tests, settings/sign-in/sign-in-list/coin-admin/RPS/coin-reset/shop service/repository/handler tests, coin-game transaction/lifecycle/race/shutdown tests, config/command-sync/preflight gate tests, daily-reset CLI/repository/worker/lease/race tests, work-payout CLI/repository/Mongo-idempotency/worker/lease/overlap/shutdown tests | `/代幣查詢` read-only query is implemented behind `MHCAT_FEATURE_ECONOMY_QUERY_ENABLED`; `/代幣排行榜` is implemented behind `MHCAT_FEATURE_ECONOMY_COIN_RANK_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_COIN_RANK`, renders a legacy-style PNG with pagination buttons, and reads `coins` without writes; `my-profile` is implemented behind `MHCAT_FEATURE_ECONOMY_PROFILE_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_PROFILE`, renders the legacy-style `user-info.png` profile card plus refresh button, and reads `coins`, `gift_changes`, `text_xps`, `voice_xps`, `work_sets`, and `work_users` without writes; `/簽到` and read-only `/簽到列表` are implemented behind `MHCAT_FEATURE_ECONOMY_SIGNIN_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_SIGNIN`; `/簽到列表` preserves the legacy public embed plus `discord.txt` export and reads `coins`/`gift_changes` without writes; `/coin-related-settings` is implemented behind `MHCAT_FEATURE_ECONOMY_SETTINGS_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_SETTINGS`; `/代幣增加` is implemented behind `MHCAT_FEATURE_ECONOMY_COIN_ADMIN_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_COIN_ADMIN`, writes one `coins` row, and preserves signed amounts, uncapped creation, scalar arithmetic, and operation-specific bounds; it should stay staging-only against disposable data; `/剪刀石頭布` is implemented behind `MHCAT_FEATURE_ECONOMY_RPS_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_RPS`, writes one existing `coins` row, preserves scalar affordability/arithmetic and duplicate ambiguity, subtracts floored half-wagers on ties, and intentionally does not cap post-win balances; `/代幣遊戲` is implemented behind `MHCAT_FEATURE_ECONOMY_GAME_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_GAME`, transactionally reserves and settles two-player wagers for `21點`, `知識王`, and `比大小`, preserves the blackjack/higher-lower `-1` wager quirk, uses the legacy topic bank/component IDs, settles knowledge/blackjack forfeits with generation timers, removes timed-out components, and serializes terminal timer/button races; `/代幣重製` is implemented behind `MHCAT_FEATURE_ECONOMY_COIN_RESET_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_COIN_RESET`, requires guild owner plus gateway/Guild Messages/Message Content for `^確認^`, and deletes or divides all guild `coins` rows only against disposable staging data; `mhcat-economy-reset` dry-run is lease-free, while apply and the separately gated `00:00 Asia/Taipei` bot worker share lease `daily-reset`; `mhcat-work-payout` can dry-run/apply completed work payout, and its separately gated every-minute worker shares the configured payout lease; production still needs duplicate audits, a unique-key strategy, game restart reconciliation, marker compatibility review, and exclusive Node/Go ownership; work payout and XP coin rewards are implemented and tested; live Discord, disposable Mongo, restart/ownership, and production-write approval remain rollout gates |
| Gacha/shop | `slashCommands/扭蛋系統/*`, `ghp_shop.js` | `internal/core/services/gacha`, `internal/core/services/economy`, `internal/adapters/mongo/repositories`, `internal/discord/features/gacha`, `internal/discord/features/economy` | `gifts`, `gift_changes`, `ghps`, `coins` | interactions/DMs | parity-audited | [49-gacha-prize-list.md](49-gacha-prize-list.md), [104-economy-shop.md](104-economy-shop.md), definition/handler/service/repository tests, scalar and duplicate contracts, guarded Mongo tests, staging gates, and race coverage | Draw, pool maintenance, and shop behavior are implemented with legacy UI, scalar coercion, duplicate-row semantics, role/DM/notification side effects, and documented safety fixes; production still requires exclusive ownership, live Discord smoke, disposable Mongo execution, data audit/repair policy, backup/rollback rehearsal, and economy-write approval |
| Work system | `work_set.js`, `handler/gift.js` | `internal/core/services/work`, `internal/adapters/mongo/repositories`, `internal/discord/features/work`, `internal/jobs`, `cmd/mhcat-work-payout` | `work_set`, `work_something`, `work_user`, `coin`, `gift_change`, `mhcat_scheduler_locks` | interactions/buttons/scheduler | parity-audited | [105-work-system.md](105-work-system.md), feature/sync gates, exact UI tests, scalar/duplicate/order contracts, guarded Mongo tests, payout dry-run/apply/worker/lease/crash tests, race coverage | command/admin/start/grant/payout behavior is implemented with legacy scalar and one-row duplicate semantics; atomic payout markers, requester-bound components, target-user creation, and lease ownership are documented safety fixes; production still requires exclusive ownership, live Discord smoke, disposable Mongo execution, backup/rollback rehearsal, and economy-write approval |
| XP text/voice | `text_xp.js`, `reset_xp.js`, `text_set.js`, `text_set_delete.js`, `text_leave_role.js`, `voice_xp.js`, `voice_set.js`, `voice_set_delete.js`, `voice_leavel_role.js`, XP commands and events | `internal/core/services/xp`, `internal/adapters/mongo/repositories`, `internal/discord/features/xp` | `text_xps`, `voice_xps`, `text_xp_channels`, `voice_xp_channels`, `chat_roles`, `voice_roles`, `coins`, `gift_changes` | disabled profile responses, configuration/admin interactions, text and voice accrual, announcements, role/coin side effects, reset, and leaderboard PNGs | parity-audited | [110-xp-system.md](110-xp-system.md), focused contracts [50](50-text-xp-config.md), [51](51-voice-xp-config.md), and [68](68-xp-reward-role-config.md) through [71](71-xp-rank.md), definition/UI/service/repository/worker tests, staging gates, and race coverage | Active legacy profile commands return the replacement embed before their unreachable card code, which Go preserves; accrual, configuration, reward roles, coin grants, admin/reset, and leaderboard behavior are implemented with documented compatibility and safety differences; production still requires exclusive ownership, live Discord/event smoke, disposable Mongo execution, data audit, asset verification, backup/rollback rehearsal, and write approval |
| Join/leave/verification/account-age | `welcome.js`, join setup commands, `群組防護/create_hours.js` | `internal/core/services/onboarding`, `internal/adapters/mongo/documents`, `internal/adapters/mongo/repositories`, `internal/adapters/discordgo`, `internal/discord/features/onboarding` | `join_messages`, `leave_messages`, `join_roles`, `verifications`, `create_hours` | member events/roles/nickname/captcha/kick | in-progress; join-role/verification/account-age parity-audited | definition/domain/document/repository/handler/event/app/config/preflight/parity/race tests | Join-role, verification, and account-age contracts are [81](81-join-role.md), [80](80-verification.md), and [79](79-account-age.md). Welcome/leave remain separately tracked; join-message writes remain dashboard-owned. |
| Tickets | `ticket.js`, `ticket_delete.js`, `modal.js`, `Ticket System.js`, `yicket system.js` | `internal/core/domain`, `internal/core/ports`, `internal/adapters/mongo/documents`, `internal/adapters/mongo/repositories`, `internal/discord/customid`, `internal/discord/features/ticket` | `tickets` | setup/delete interactions, versioned/legacy modal panels, open/close buttons, channel/message side effects | parity-audited | definition/domain/document/repository/parser/router/handler/app/config/command-sync/preflight/parity/race tests | Exact UI/colors/raw text, Mongoose scalar reads, create-if-absent and duplicate cleanup, stale-modal rejection, identity-scoped config rollback, failed-welcome channel cleanup, all-type duplicate lookup, overwrite order/current identities, owner-close fix, usage ownership, smoke, and rollback are locked in the [ticket parity contract](74-ticket.md). |
| Voice rooms/locks | `voice_create.js`, voice room commands | `internal/discord/features/voice`, `internal/core/services/voice`, `internal/adapters/mongo/repositories` | `voice_channels`, `voice_channel_ids`, `lock_channels` | config interactions, dynamic voice-state room creation/cleanup, current voice-state lookup for lock command, and existing locked-channel join enforcement | parity-audited | definition/handler/service/repository/document/adapter/app/config/command-sync/preflight/parity/race tests | Public command definitions with runtime permissions, explicit-zero/raw-value behavior, current-parent room creation, legacy stage deletion, bot triggers, exact lock UI/deadline, and Mongoose-compatible scalar/null/array reads are locked by focused tests. Gates, safety differences, staging, ownership, and rollback are recorded in the [voice-room parity contract](72-voice-room-config.md); voice XP and voice logging remain separate runtime paths. |
| Polls | `管理系統/poll.js`, `events/poll.js` | `internal/core/domain`, `internal/core/ports`, `internal/adapters/mongo/documents`, `internal/adapters/mongo/repositories`, `internal/discord/features/poll` | `polls` | interactions/buttons/selects/attachments | parity-audited | definition/domain/document/repository/custom-ID/router/handler/artifact/app/config/command-sync/preflight/parity/race tests | Exact command metadata, UTF-16 validation, raw text, initial/rerender UI, colors, JavaScript rounding, legacy/versioned routes, Mongoose-compatible reads, atomic writes, compensation, usage ownership, smoke, and rollback are locked in the [poll parity contract](75-poll.md). |
| Lottery | `lotter_create.js`, `events/btn.js` lottery components | `internal/core/services/lottery`, `internal/adapters/mongo/repositories`, `internal/discord/features/lottery` | `lotters` existing rows; no creation writes | interactions/buttons/attachments/channel sends | parity-audited | domain/document/repository/service/parser/handler UI-boundary/race/app/config/preflight tests | `/抽獎設置` preserves the disabled response behind `MHCAT_FEATURE_LOTTERY_DISABLED_COMMAND_ENABLED`; existing numeric `lotter*` enter/search/reroll/stop buttons are independently implemented behind `MHCAT_FEATURE_LOTTERY_COMPONENTS_ENABLED`, with rollback-compatible participant/end writes, exact participant export formatting, winner sends, and documented security/reliability differences; creation and public panel generation remain disabled |
| Announcements | `announcement.js`, `announcement_set_channel.js`, `modal.js`, `ann_message.js` | `internal/core/domain`, `internal/core/services/announcements`, `internal/adapters/mongo/documents`, `internal/adapters/mongo/repositories`, `internal/discord/features/announcements` | `guilds`, `ann_all_sets` | slash interactions, modals, components, message create relay, Discord channel send/delete | parity-audited | definition/domain/service/document/repository/custom-ID/router/handler/adapter/app/config/command-sync/preflight/parity/race tests | Exact UI, permission presentation, raw text/color behavior, six-second owner/guild confirmation, Mongoose-compatible reads, typed duplicate-safe writes, relay ordering, usage ownership, mention safety, staging, and rollback are locked in the [announcement parity contract](76-announcement.md). |
| Logging | `LoggingSystem.js`, `create_logging.js` | `internal/core/services/moderation`, `internal/adapters/mongo/repositories`, `internal/discord/features/logging` | `loggings` | setup interaction/select; message edit/delete, channel update, and voice join/leave emitters | parity-audited | command definition/UI, runtime permission, owner/deadline, service, BSON scalar compatibility, repository, event conversion, exact message/channel/voice payload, audit attribution, app wiring, command-sync, preflight, and race tests | Public command registration, runtime Manage Messages, exact ten-minute owner-scoped UI, typed writes/mixed-scalar reads, active event boundaries, cached-state rules, exact action-72/action-11 attribution, topic nulls, permission order, avatar/attachment formatting, ignored moves, safety differences, independent gates, exclusive Node/Go event ownership, smoke, and rollback are locked in the [logging parity contract](48-logging-config.md). |
| Moderation/warnings | `slashCommands/警告系統/warnings.js`, `warn.js`, `erros_set.js`, `remove-warn.js`, `remove-all-warnings.js`; `clear.js`, `delete_data.js` | `internal/core/services/moderation`, `internal/adapters/discordgo`, `internal/adapters/mongo/repositories`, `internal/discord/features/moderation` | `warndbs`; `errors_sets`; nine config collections for delete-data; cleanup uses no Mongo | warning history/settings/removal/issue interactions; optional kick/ban threshold actions; message cleanup; delete-data select | warning system, message cleanup, and delete-data parity-audited | definition/service/document/repository/real-Mongo/DiscordGo HTTP/handler/app/config/command-sync/preflight/race/vet tests | Warning is locked in [84-warning-system.md](84-warning-system.md), message cleanup in [85-message-cleanup.md](85-message-cleanup.md), and delete-data in [83-delete-data.md](83-delete-data.md). Cleanup preserves exact UI/count permissions while intentionally waiting, reporting actual deletes, paginating target scans, and retaining all old messages. |
| Reaction/button roles | `role.js`, `role_delete.js`, `releadd.js`, `message_reaction.js`, `btn.js`, `modal.js` | `internal/core/domain`, `internal/core/services/roles`, `internal/adapters/mongo/documents`, `internal/adapters/mongo/repositories`, `internal/adapters/discordgo`, `internal/discord/features/roles` | `message_reactions`, `btns` | setup/delete interactions, modal/panel, buttons, reaction events, REST role writes | parity-audited | definition/domain/service/document/repository/cache/REST/parser/router/handler/app/config/command-sync/preflight/parity/race tests | Exact public/ephemeral UI, URL/emoji quirks, JavaScript-shaped IDs, cached hierarchy order, Mongoose scalar reads, duplicate-safe writes, custom-emoji delete fix, event failure classes/icons, same-process bot retention, post-restart limitation, one ownership gate, usage, smoke, and rollback are locked in the [role-selection parity contract](73-role-selection.md). |
| Anti-scam/safe server | `safe_server.js`, `not_a_goodweb.js`, `report_web.js`, `SlashCommands.js` | `internal/core/domain`, `internal/core/services/safety`, `internal/adapters/mongo/documents`, `internal/adapters/mongo/repositories`, `internal/adapters/external`, `internal/discord/features/safety` | `good_webs`, `not_a_good_webs` | config interaction, report webhook, message scan/delete/warning | parity-audited | definition/domain/service/document/repository/webhook/handler/event/app/config/command-sync/preflight/parity/race tests | Exact UI/permissions, pinned URL rules, raw text, Mongoose scalar reads, typed duplicate-safe writes, webhook safety, global usage ownership, bot scanning, delete/warning ordering, intents, ownership, smoke, and rollback are locked in the [anti-scam parity contract](77-anti-scam.md); account-age remains separate. |
| Auto notifications | cron commands, modal/select handlers, `handler/cron.js` | `internal/core/services/notifications`, `internal/adapters/mongo/repositories`, `internal/discord/features/notifications`, `internal/adapters/mongo.SchedulerLeaseStore` | `cron_sets`, `mhcat_scheduler_locks` | setup/list/delete interactions; lease-backed scheduled sends | parity-audited | config/delivery domain, document, repository, real-Mongo, handler, wizard, worker, app, usage, command-sync, preflight, lease, race, and fake-time tests | Exact command/UI, cron grammar, BSON scalar/Mixed behavior, guild-bound delivery, ownership, staging, and rollback are locked in [92-auto-notification.md](92-auto-notification.md). Config and delivery remain independently disabled by default; Node `handler/cron.js` must be stopped before Go delivery. |
| Scheduled economy jobs | `handler/cron.js`, `handler/gift.js` | `internal/jobs`, `cmd/mhcat-economy-reset`, `cmd/mhcat-work-payout`, `internal/adapters/mongo.SchedulerLeaseStore` | `coins`, `work_*`, `gift_changes`, `mhcat_scheduler_locks` | lease-backed daily reset and work payout | in-progress | daily-reset and work-payout dry-run/apply/worker, lease, race, and fake-time tests | Economy daily reset one-shot apply and recurring midnight worker share `daily-reset`; recurring work payout shares its configured lease between CLI apply and the every-minute worker. Production ownership and smoke remain separate from auto-notification parity. |
| Birthday | `birthday.js`, `birthday_set.js`, `birthday.js` model, commented `handler/gift.js` block, `SlashCommands.js` | `internal/core/domain`, `internal/core/services/birthday`, `internal/adapters/mongo/documents`, `internal/adapters/mongo/repositories`, `internal/discord/features/birthday` | `birthdays`, `birthday_sets` | config/add selectors/preference/delete/list; delivery inactive | parity-audited | definition/domain/service/document/repository/handler/state/app/config/command-sync/preflight/parity/race tests | Exact metadata, effective permissions, validation/UI, five-minute selectors, scalar reads, typed duplicate-safe writes, global usage ownership, intentional data-safety fixes, inactive delivery boundary, ownership, staging, and rollback are locked in the [birthday parity contract](78-birthday.md). |
| Stats channels | stats commands, `channel_status.js` | `internal/core/services/stats`, `internal/adapters/mongo/repositories`, `internal/discord/features/stats` | `numbers`, `role_numbers` | static interaction reply, channel create, config-row delete, channel rename worker | parity-audited | definition, domain/document/repository/real-Mongo/service, exact handler payload, Discord cache adapter, worker cadence/lifecycle, scalar/whitespace, usage ownership, race, app wiring, config, command-sync, and preflight tests | Exact UI, cached count/type quirks, channel overwrites, Mongoose scalar/whitespace behavior, BSON null shape, duplicate boundaries, worker lifecycle, ownership, staging, and rollback are locked in [93-stats.md](93-stats.md). |
| Operational tools | none legacy equivalent | `cmd/mhcat-command-sync`, `cmd/mhcat-mongo-audit`, `cmd/mhcat-mongo-index`, `cmd/mhcat-staging-preflight`, `cmd/mhcat-economy-reset`, `cmd/mhcat-work-payout`, `cmd/mhcat-scheduler-lease`, `tools/parity-audit`, future `cmd/mhcat-tools` | all | command registration/index/audit | in-progress | CLI dry-run, audit, index diff, staging preflight, daily-reset dry-run/apply/lease/worker guards, work-payout dry-run/apply/worker guards, scheduler lease CLI/tests, catalog contract tests, slash-command UI parity audit tests | command sync dry-run exists; slash-command UI parity audit reports 74 matching Go definitions with zero drift/missing names; staging preflight exists; Mongo audit and index dry-run/apply tools exist; economy daily reset has coordinated one-shot and recurring paths; work payout has coordinated one-shot and recurring paths; scheduler lease diagnostic CLI exists; full 47-model Mongo catalog exists; data repair still not-started |
