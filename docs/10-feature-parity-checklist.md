# Feature Parity Checklist

Status: Ticket config repository foundation, ticket setup/delete UI handlers, `tic`/`del` open/close side-effect handlers, runtime feature flag, and staging command-sync include gate are implemented after Platform Wave B. A repeatable slash-command UI parity audit now compares all 74 legacy slash command definitions against current Go definitions; the current report shows 74 matching Go definitions, zero UI metadata drift, and no legacy command names without Go definitions. Poll Wave A/B now implements gated poll create/vote/owner-menu/result chart/text export/Excel export handlers with legacy-style visible UI and safer repository vote semantics. Low-risk utility commands `help`, `ping`, `info bot`, `info shard`, `info user`, and `info guild` are implemented and tested. `/help`, `/info bot`, `/info shard`, `/info user`, and `/info guild` now render legacy-style interfaces, with `/info shard` intentionally fixing the old empty initial embed. Read-only `/代幣查詢`, read-only `/代幣排行榜`, and read-only `my-profile` are implemented behind explicit economy feature flags. `/簽到` has a disabled-by-default, staging-gated first write slice with legacy-style loading/image/buttons, and `/簽到列表` is implemented as a read-only list/export path under the same gate. `/代幣增加`, `/剪刀石頭布`, and `/代幣重製` are implemented as disabled-by-default, staging-gated coin write slices; `/代幣增加` preserves admin adjustment guards, `/剪刀石頭布` preserves legacy win/loss/tie wager behavior, and `/代幣重製` preserves the owner-only `^確認^` destructive reset flow. `/代幣遊戲` is implemented behind explicit economy game runtime and command-sync gates with legacy two-player wager game UI, and `/代幣商店` is implemented behind explicit economy shop runtime and command-sync gates with legacy shop setup/query/purchase UI; Role-selection slash commands, legacy `nal` role-button modal/button routing, and reaction add/remove role events are implemented behind explicit runtime, command-sync, gateway, and Guild Message Reactions intent gates. The economy daily reset now has a lease-gated one-shot apply path and a separately gated recurring worker, while work payout now has crash-idempotent one-shot and separately gated recurring paths. `打工系統` now has a gated command-definition shell, exact legacy dashboard redirect for `新增打工事項`, legacy-style `打工介面` list/captcha/detail/start UI with an explicit atomic `work_users` start-write path, and admin setup/delete/energy writes behind explicit admin repository wiring. `/警告紀錄`, `/警告設定`, `/警告清除`, `/警告全部清除`, and `/警告` are implemented behind separate warning runtime and staging command-sync gates; `/警告` appends `warndbs`, sends legacy-style DMs, and can run configured kick/ban threshold actions from `errors_sets`. `/刪除訊息` is implemented behind explicit message-cleanup runtime and staging command-sync gates, deletes recent Discord messages only, and writes no Mongo data. `/刪除資料` is implemented behind explicit delete-data runtime and staging command-sync gates, preserves the legacy destructive select UI, and deletes selected guild-scoped legacy config rows. `/翻譯` is implemented behind explicit translate runtime and staging command-sync gates. Read-only `/查看餘額` and write `/兌換` are implemented behind separate explicit runtime and staging command-sync gates. `/自動聊天頻道` and `/自動聊天頻道刪除` are implemented behind explicit config/runtime gates; separately gated local and paid MessageCreate runtimes restore the legacy balance split and Mongo worker handoff. `automatic-notification`, `/自動通知列表`, and `/自動通知刪除` are implemented as auto-notification config-maintenance commands behind explicit runtime and staging command-sync gates; setup creates pending `cron_sets` rows, completes direct cron modal submits and owner-scoped simplified weekday/hour/minute selections, writes rollback-compatible message payloads, and sends setup previews; a separately gated lease-backed recurring delivery worker restores scheduled sends. `/防詐騙網址` is implemented as a config-only `good_webs` toggle behind explicit runtime and staging command-sync gates; `/詐騙網址回報` is implemented behind explicit runtime, command-sync, and webhook gates; message deletion is implemented behind a separate gateway runtime gate. `/語音包廂設置` and `/語音包廂刪除` are implemented as `voice_channels` maintenance commands, dynamic trigger joins now create/move/delete tracked voice rooms with `voice_channel_ids` and owner/category permission overwrites when gateway Voice State events are enabled, and `/上鎖頻道` plus existing passworded `lock_channels` join enforcement are implemented behind explicit runtime and staging command-sync/gateway gates. `/set-log-channel` now has a gated logging-config slice with legacy-style visible UI and rollback-compatible `loggings` writes. `/扭蛋`, `/扭蛋獎池查詢`, `/扭蛋獎池增加`, `/扭蛋獎品編輯`, and `/扭蛋獎池刪除` are implemented behind explicit gacha runtime and staging command-sync gates; `/扭蛋` mutates staging `coins`/`gifts`, can send prize-code DMs and notification-channel messages, and remains disabled by default; shop writes are separately gated. `/抽獎設置` preserves the legacy disabled-command unavailable response behind explicit runtime and command-sync gates; existing lottery-message enter/search/reroll/stop buttons are independently implemented behind a component runtime gate, while creation remains disabled. `/統計系統查詢` now preserves the legacy static stats help embed, `/統計系統創建` creates legacy stats channels and writes `numbers`, `/統計身分組人數` creates role-count channels and writes `role_numbers`, `/統計系統刪除` deletes legacy `numbers` config rows behind explicit runtime and command-sync gates, and the event-only stats rename worker updates `numbers`/`role_numbers` stat channels behind `MHCAT_FEATURE_STATS_RENAME_WORKER_ENABLED`; all stats mutation paths remain disabled by default. `/聊天經驗` and `/語音經驗` now preserve the legacy disabled-command replacement response behind explicit runtime and command-sync gates; XP profile card rendering remains disabled. Config-only `聊天經驗設定`, `聊天經驗刪除`, `語音經驗設定`, `語音經驗刪除`, text XP accrual profile writes, configured level-up announcements, text reward-role changes, and XP coin rewards, voice XP session tracking, voice XP 30-second runtime loop, voice XP restart reconciliation, `聊天經驗身分組設定`, `語音經驗身分組設定`, gated `/經驗值改變` XP admin writes, `/經驗值重製` XP deletes, read-only `/聊天排行榜` and `/語音排行榜` XP leaderboard PNGs, `/驗證設置`, gated `/驗證` captcha/role/nickname flow, `/帳號需創建時數`, `/生日系統 祝福語設定`, `/生日系統 增加`, `/生日系統 是否允許管理員設定`, `/生日系統 刪除`, `/生日系統 生日列表`, and the gated member-add account-age kick/log flow are implemented behind explicit runtime and staging command-sync gates; XP profile cards and birthday notifications remain disabled. Ticket, poll, economy-query, economy coin-rank, economy RPS, economy game, economy shop, economy profile, economy sign-in, economy coin-admin, economy coin-reset, role-selection, work, warning-history, warning-settings, warning-removal, warning-issue, message-cleanup, delete-data, translate, balance-query, redeem, autochat-config, autochat-fallback, autochat-paid-handoff, auto-notification-config, auto-notification-delivery, daily-reset-scheduler, anti-scam-config, anti-scam-report, anti-scam-message-delete, voice-room config, voice-room lock, logging-config, gacha prize-list, gacha draw, gacha prize-create, gacha prize-edit, gacha prize-delete, lottery disabled-command, lottery components, stats-query, stats-create, stats-role-count, stats-delete, stats-rename-worker, birthday-config/profile, text-XP config, text-XP accrual, voice-XP config, voice-XP sessions, XP reward-role config, XP profile disabled-command, XP admin, XP reset, XP rank, verification config, verification flow, and account-age runtime side effects remain disabled by default and must be paired with staging-only command sync or event-only smoke gates before live use. The external ChatGPT worker remains an operator-confirmed dependency; birthday delivery and XP profile cards stay inactive because those legacy paths are commented out or unreachable.

| Feature | Legacy file | New Go package | Mongo collections | Discord APIs | Status | Tests | Notes |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Startup and lifecycle | `shard.js`, `index.js` | `internal/app`, `internal/discord/session` | none direct | Gateway/sharding | in-progress | lifecycle/shutdown/runtime wiring tests | Gateway opens only when explicitly enabled; DiscordGo sharding differs from discord.js |
| Config/secrets | `.env.template`, `config.json`, `index.js` | `internal/config` | none | webhooks | not-started | env alias/redaction tests | remove hardcoded webhook/admin IDs |
| Command registration infrastructure | `handler/slash_commands.js` | `cmd/mhcat-command-sync`, `internal/discord/commands`, `internal/adapters/discordgo` | none | application commands REST | in-progress | diff/dry-run/fake client/staging guard tests | separate CLI only; no startup global mutation; staging apply completed guild-only for managed `help`, `ping`, and `info` |
| Slash dispatch | `events/SlashCommands.js` | `internal/discord/runtime`, `internal/discord/interactions`, `internal/adapters/mongo/repositories` | `all_use_counts` (gated) | interactions | in-progress | router/adapter/dispatcher/wiring/repository/config/preflight tests | Global slash tracking is disabled by default; `MHCAT_FEATURE_USAGE_TRACKING_ENABLED=true` atomically increments once before route lookup, permission checks, and handler work; feature handlers do not double count |
| Shared Discord platform | `events/*.js`, Discord REST calls in feature files | `internal/discord/events`, `internal/core/ports`, `internal/adapters/discordgo` | none | messages/reactions/members/voice/channels/roles/audit logs | in-progress | event dispatcher, intent, responder, side-effect adapter tests | Platform Wave A shell complete; no feature owns these side effects yet; event intents remain opt-in |
| Component/modal dispatch | `events/btn.js`, `modal.js`, `poll.js`, `rank.js` | `internal/discord/customid`, `internal/discord/interactions`, `internal/discord/responses` | many | buttons/selects/modals | in-progress | custom ID golden/collision tests, modal response tests, ticket modal tests, poll/rank/role route tests | parser avoids broad matching; ticket setup modal path and legacy `nal` ticket panel-submit route implemented; poll component routes, rank pagination routes, role-button modal/buttons, sign/profile refresh, and economy game buttons are implemented behind their feature gates; remaining legacy components are tracked in the component matrix |
| Utility commands | `ping.js`, `info.js`, `help.js`, `translate.js` | `internal/core/services/utility`, `internal/discord/features/utility`, `internal/adapters/external` | none for current utility slice | interactions/selects/buttons/external translate HTTP | in-progress | service, handler, golden, registry, dry-run, runtime, staging guard tests | implemented and runtime-routable: `help`, `ping`, `info bot`, `info shard`, `info user`, `info guild`; `/翻譯` is implemented behind `MHCAT_FEATURE_TRANSLATE_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_TRANSLATE=true`; `/help`, `/info bot`, `/info shard`, `/info user`, `/info guild`, and `/翻譯` legacy UI restored or matched; auto-chat config and local fallback are tracked separately; paid ChatGPT runtime excluded |
| Auto chat | `chat.js`, `chat_delete.js`, `events/Chatbot.js`; balance/redeem commands `check_price.js`, `get_something.js` | `internal/core/services/autochat`, `internal/core/services/utility`, `internal/core/services/redeem`, `internal/adapters/mongo/repositories`, `internal/discord/features/autochat`, `internal/discord/features/balance`, `internal/discord/features/redeem` | `chats`, `chatgpts`, `chatgpt_gets`, `codes` | config interactions, balance query, redeem credit, local replies, and transactional paid worker handoff | in-progress | domain/service/document/repository/handler/app/command-sync/preflight and replica-set Mongo transaction tests | `/自動聊天頻道` and `/自動聊天頻道刪除` write/delete legacy-compatible `chats` config behind `MHCAT_FEATURE_AUTOCHAT_CONFIG_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_AUTOCHAT_CONFIG=true`; `/查看餘額` reads `chatgpt_gets.price` behind `MHCAT_FEATURE_BALANCE_QUERY_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_BALANCE_QUERY=true`; `/兌換` consumes `codes`, credits `chatgpt_gets.price`, and preserves legacy 7-day expiry and ephemeral embeds behind `MHCAT_FEATURE_REDEEM_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_REDEEM=true`; the local fallback is implemented behind `MHCAT_FEATURE_AUTOCHAT_FALLBACK_ENABLED=true` plus gateway/Guild Messages/Message Content; the paid path is implemented behind `MHCAT_FEATURE_AUTOCHAT_PAID_HANDOFF_ENABLED=true` plus ownership confirmation, atomically debits `chatgpt_gets`, publishes the legacy `chatgpts` request, preserves 10/40-second timing and mention warnings, and still requires the separately deployed external worker |
| Economy/sign/profile | `slashCommands/代幣系統/*` | `internal/core/services/economy`, `internal/adapters/mongo/repositories`, `internal/discord/features/economy` | `coins`, `gift_changes`, `sign_lists`, `work_*`, XP collections | interactions/buttons/DMs | in-progress | BSON decode tests, read-only query/rank/profile service tests, settings/sign-in/sign-in-list/coin-admin/RPS/coin-reset/shop service/repository/handler tests, config/command-sync/preflight gate tests, daily-reset CLI/repository/worker/lease/race tests, work-payout CLI/repository/Mongo-idempotency/worker/lease/overlap/shutdown tests | `/代幣查詢` read-only query is implemented behind `MHCAT_FEATURE_ECONOMY_QUERY_ENABLED`; `/代幣排行榜` is implemented behind `MHCAT_FEATURE_ECONOMY_COIN_RANK_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_COIN_RANK`, renders a legacy-style PNG with pagination buttons, and reads `coins` without writes; `my-profile` is implemented behind `MHCAT_FEATURE_ECONOMY_PROFILE_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_PROFILE`, renders the legacy-style `user-info.png` profile card plus refresh button, and reads `coins`, `gift_changes`, `text_xps`, `voice_xps`, `work_sets`, and `work_users` without writes; `/簽到` and read-only `/簽到列表` are implemented behind `MHCAT_FEATURE_ECONOMY_SIGNIN_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_SIGNIN`; `/簽到列表` preserves the legacy public embed plus `discord.txt` export and reads `coins`/`gift_changes` without writes; `/coin-related-settings` is implemented behind `MHCAT_FEATURE_ECONOMY_SETTINGS_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_SETTINGS`; `/代幣增加` is implemented behind `MHCAT_FEATURE_ECONOMY_COIN_ADMIN_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_COIN_ADMIN`, writes `coins`, rejects negative and over-`999999999` balances, and should stay staging-only against disposable data; `/剪刀石頭布` is implemented behind `MHCAT_FEATURE_ECONOMY_RPS_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_RPS`, writes existing `coins` rows, rejects missing/insufficient balances, subtracts floored half-wagers on ties, and intentionally does not cap post-win balances; `/代幣遊戲` is implemented behind `MHCAT_FEATURE_ECONOMY_GAME_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_GAME`, reserves and settles two-player wagers for `21點`, `知識王`, and `比大小`, uses the legacy topic bank and component IDs, and intentionally validates the invited user before debiting; `/代幣重製` is implemented behind `MHCAT_FEATURE_ECONOMY_COIN_RESET_ENABLED` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_COIN_RESET`, requires guild owner plus gateway/Guild Messages/Message Content for `^確認^`, and deletes or divides all guild `coins` rows only against disposable staging data; `mhcat-economy-reset` dry-run is lease-free, while apply and the separately gated `00:00 Asia/Taipei` bot worker share lease `daily-reset`; `mhcat-work-payout` can dry-run/apply completed work payout, and its separately gated every-minute worker shares the configured payout lease; production still needs duplicate audits, a unique-key strategy, marker compatibility review, and exclusive Node/Go payout ownership; game background timeout edits, work payout production ownership, XP rewards remain not-started |
| Gacha/shop | `slashCommands/扭蛋系統/*`, `ghp_shop.js` | `internal/core/services/gacha`, `internal/core/services/economy`, `internal/adapters/mongo/repositories`, `internal/discord/features/gacha`, `internal/discord/features/economy` | `gifts`, `gift_changes`, `ghps`, `coins` | interactions/DMs | in-progress | command definition, service, BSON document, handler UI, app wiring, command-sync/preflight gate tests | `/扭蛋獎池查詢` read-only prize-pool query is implemented behind `MHCAT_FEATURE_GACHA_PRIZE_LIST_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_PRIZE_LIST=true`; it reads `gifts`/`gift_changes` only, preserves legacy embed text including `gift_chence`, and splits >25 prize fields as a Discord API bug fix; `/扭蛋` is implemented behind `MHCAT_FEATURE_GACHA_DRAW_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_DRAW=true`, preserves legacy draw-count/cost choices and result messaging, reads draw cost and notification config from `gift_changes`, charges `coins`, and decrements or deletes auto-delete `gifts` rows, and sends prize-code DMs plus notification-channel messages best-effort; draw intentionally applies one inventory decrement/delete per drawn auto-delete prize instead of the legacy duplicate async decrement loops; `/扭蛋獎池增加` is implemented behind `MHCAT_FEATURE_GACHA_PRIZE_CREATE_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_PRIZE_CREATE=true`, requires Manage Messages, and inserts one legacy-compatible `gifts` row after duplicate and pool-size checks; `/扭蛋獎品編輯` is implemented behind `MHCAT_FEATURE_GACHA_PRIZE_EDIT_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_PRIZE_EDIT=true`, requires Manage Messages, and replaces one legacy-compatible `gifts` row by `{guild,gift_name}` using legacy merge quirks; `/扭蛋獎池刪除` is implemented behind `MHCAT_FEATURE_GACHA_PRIZE_DELETE_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_GACHA_PRIZE_DELETE=true`, requires Manage Messages, and deletes one `gifts` row by `{guild,gift_name}`; `/代幣商店` is implemented behind `MHCAT_FEATURE_ECONOMY_SHOP_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_ECONOMY_SHOP=true`, writes `ghps` and `coins`, can add roles and DM prize codes, and must stay staging-only against disposable shop/coin rows |
| Work system | `work_set.js`, `handler/gift.js` | `internal/core/services/work`, `internal/adapters/mongo/repositories`, `internal/discord/features/work`, `internal/jobs`, `cmd/mhcat-work-payout` | `work_set`, `work_something`, `work_user`, `coin`, `gift_change`, `mhcat_scheduler_locks` | interactions/buttons/scheduler | in-progress | feature flag, command definition, repository/document tests, explicit start/admin-repository tests, service tests, handler UI, app wiring, command-sync/preflight gate, fake-time/payout, work-payout dry-run/apply/worker/lease/overlap/shutdown tests | crash-idempotent one-shot and recurring completed-work payout paths exist; `打工系統` command definition, exact legacy `新增打工事項` dashboard redirect, legacy-style `打工介面` list/captcha/detail/start UI, and setup/delete/energy admin writes are implemented behind `MHCAT_FEATURE_WORK_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_WORK=true`; start and energy updates use explicit atomic repository paths; recurring payout is implemented behind independent Gateway/write/lease gates; production ownership still requires duplicate audits and rollout testing |
| XP text/voice | `text_xp.js`, `reset_xp.js`, `text_set.js`, `text_set_delete.js`, `text_leave_role.js`, `voice_xp.js`, `voice_set.js`, `voice_set_delete.js`, `voice_leavel_role.js`, XP commands | `internal/core/services/xp`, `internal/adapters/mongo/repositories`, `internal/discord/features/xp` | `text_xps`, `voice_xps`, `text_xp_channels`, `voice_xp_channels`, `chat_roles`, `voice_roles`, `coins`, `gift_changes` | disabled profile responses, config interactions, text accrual profile writes, text level-up announcement/fallback sends, text reward-role changes, text XP coin rewards, voice session state, voice tick service, 30-second runtime loop, and level-up side effects, XP admin adjustment, XP reset deletes, and read-only leaderboard PNGs only; profile-card render pending | in-progress | domain/service/repository/definition/handler/app/config/command-sync/preflight tests for disabled profile responses, config slices, reward-role config, XP admin, XP reset, XP rank, voice XP sessions, text XP accrual, and voice tick side effects and runtime worker | `聊天經驗`/`語音經驗` preserve the legacy disabled replacement embed behind `MHCAT_FEATURE_XP_PROFILE_DISABLED_COMMANDS_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_XP_PROFILE_DISABLED_COMMANDS=true`; `聊天經驗設定`/`聊天經驗刪除` and `語音經驗設定`/`語音經驗刪除` are implemented behind separate runtime and command-sync flags; text XP accrual is implemented behind `MHCAT_FEATURE_TEXT_XP_ACCRUAL_ENABLED=true` plus gateway/Guild Messages/Message Content, updates rollback-compatible `text_xps` rows, sends configured/default level-up announcements when a `text_xp_channels` row exists, sends legacy fallback errors when that channel is missing or unusable, applies configured `chat_roles` reward-role changes, and grants legacy XP coin rewards from `gift_changes.xp_multiple` after the configured announcement path succeeds; voice XP session tracking is implemented behind `MHCAT_FEATURE_VOICE_XP_SESSIONS_ENABLED=true` plus gateway/Voice State intent and marks rollback-compatible `voice_xps.leavejoin=join/leave` rows, starts one legacy 30-second XP loop per joined user, and reconciles existing `leavejoin:"join"` rows on startup; the voice XP runtime preserves legacy `+5 XP` ticks, `xp:"5"` on level-up, configured/default level-up announcements, owner DM fallbacks, `voice_roles` changes, and XP coin rewards after the configured announcement path succeeds; `聊天經驗身分組設定` and `語音經驗身分組設定` are implemented behind `MHCAT_FEATURE_XP_ROLE_CONFIG_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_XP_ROLE_CONFIG=true`, writing rollback-compatible `chat_roles`/`voice_roles` rows with legacy `leavel` strings and pagination buttons; `/經驗值改變` is implemented behind `MHCAT_FEATURE_XP_ADMIN_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_XP_ADMIN=true`, requires Kick Members, and writes rollback-compatible `text_xps`/`voice_xps` profile rows with legacy `xp`/`leavel` strings and `voice_xps.leavejoin=leave` on insert; `/經驗值重製` is implemented behind `MHCAT_FEATURE_XP_RESET_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_XP_RESET=true`, requires guild owner, gateway/Guild Messages/Message Content intents for full-reset confirmation, and deletes rollback-compatible `text_xps`/`voice_xps` rows; `/聊天排行榜` and `/語音排行榜` are implemented behind `MHCAT_FEATURE_XP_RANK_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_XP_RANK=true`, read `text_xps`/`voice_xps`, and render legacy-style `user-info.png` leaderboard pages with legacy buttons; Go writes rollback-compatible `text_xp_channels` and `voice_xp_channels` config and suppresses preview/success mentions; profile rank cards under `/聊天經驗`/`/語音經驗` remain not-started |
| Join/leave/verification/account-age | `welcome.js`, join setup commands, `群組防護/create_hours.js` | `internal/core/services/onboarding`, `internal/adapters/mongo/repositories`, `internal/discord/features/onboarding` | `join_messages`, `leave_messages`, `join_roles`, `verifications`, `create_hours` | member events/roles/nickname/captcha/kick | in-progress | join-role, welcome-message, verification config/flow, account-age config/policy service/repository/handler/app/command-sync/preflight tests; join-role assignment, account-age gate, welcome-message delivery, and leave-message delivery event tests | `/加入身份組設置` and `/加入身份組刪除` config writes are implemented behind `MHCAT_FEATURE_JOIN_ROLE_CONFIG_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_JOIN_ROLE_CONFIG=true`; member-add join-role assignment is implemented behind `MHCAT_FEATURE_JOIN_ROLE_ASSIGNMENT_ENABLED=true` plus explicit gateway/Guild Members intent flags; `/加入訊息設置` dashboard redirect and `/退出訊息設置` legacy modal/config writes are implemented behind `MHCAT_FEATURE_WELCOME_MESSAGE_CONFIG_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_WELCOME_MESSAGE_CONFIG=true`; member-add welcome-message delivery is implemented behind `MHCAT_FEATURE_WELCOME_MESSAGE_DELIVERY_ENABLED=true` plus explicit gateway/Guild Members intent flags and reads existing dashboard/legacy `join_messages` rows without writing; member-remove leave-message delivery is implemented behind `MHCAT_FEATURE_LEAVE_MESSAGE_DELIVERY_ENABLED=true` plus explicit gateway/Guild Members intent flags; `/驗證設置` writes only legacy-compatible `verifications` config behind `MHCAT_FEATURE_VERIFICATION_CONFIG_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_VERIFICATION_CONFIG=true`; `/驗證` captcha prompt, button, modal, role assignment, and optional nickname template are implemented behind `MHCAT_FEATURE_VERIFICATION_FLOW_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_VERIFICATION_FLOW=true`; `/帳號需創建時數` writes only legacy-compatible `create_hours` config behind `MHCAT_FEATURE_ACCOUNT_AGE_CONFIG_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_ACCOUNT_AGE_CONFIG=true`; account-age member-add DM/kick/log gate is implemented behind `MHCAT_FEATURE_ACCOUNT_AGE_POLICY_ENABLED=true`, gateway, and Guild Members intent; Go-generated verification IDs use state IDs instead of embedding the captcha answer, while legacy `<captcha>verification`/`<captcha>ver` IDs remain compatible; join-message config writes remain not-started because the current legacy slash command redirects to dashboard |
| Tickets | `Ticket System.js`, `yicket system.js`, ticket commands | `internal/core/domain`, `internal/core/ports`, `internal/adapters/mongo/documents`, `internal/adapters/mongo/repositories`, `internal/discord/features/ticket` | `tickets` | interactions/modals/buttons/channels | in-progress | BSON fixture, fake repository contract, adapter constructor tests, ticket setup/delete handler tests, modal conversion tests, fake side-effect tests, dispatcher route tests, config/command-sync guard tests, color-name parity tests | setup/delete UI handlers, legacy duplicate/permission errors, versioned and legacy `nal` modal panel-submit paths, CSS/HTML named ticket panel colors, and `tic`/`del` side-effect handlers implemented; runtime enablement requires `MHCAT_FEATURE_TICKETS_ENABLED=true`; command sync includes tickets only with staging guild `MHCAT_COMMAND_SYNC_INCLUDE_TICKETS=true` |
| Voice rooms/locks | `voice_create.js`, voice room commands | `internal/discord/features/voice`, `internal/core/services/voice`, `internal/adapters/mongo/repositories` | `voice_channels`, `voice_channel_ids`, `lock_channels` | config interactions, dynamic voice-state room creation/cleanup, current voice-state lookup for lock command, and existing locked-channel join enforcement | in-progress | definition/handler/app/config/command-sync/preflight/adapter tests for config, dynamic room, and lock slices | `/語音包廂設置` and `/語音包廂刪除` write/delete legacy-compatible `voice_channels` behind `MHCAT_FEATURE_VOICE_ROOM_CONFIG_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_VOICE_ROOM_CONFIG=true`; when Gateway Voice State events are enabled, trigger joins create legacy-named voice rooms, copy parent permission overwrites plus owner management permissions, persist `voice_channel_ids`, seed lockable `lock_channels` rows, move the member, and delete empty tracked dynamic rooms; `/上鎖頻道` replaces an existing `lock_channels` row behind `MHCAT_FEATURE_VOICE_ROOM_LOCK_ENABLED=true`, Gateway, Voice State intent, and `MHCAT_COMMAND_SYNC_INCLUDE_VOICE_ROOM_LOCK=true`; passworded existing `lock_channels` rows send the legacy-style prompt, disconnect unauthorized users, DM instructions, open the legacy `<channel>anser` modal from the generated prompt button, append correct users to `ok_people`, and return the legacy channel-link response; voice XP and voice logging are implemented under separate runtime gates and are not enabled by voice-room config |
| Polls | `管理系統/poll.js`, `events/poll.js` | `internal/core/domain`, `internal/core/ports`, `internal/adapters/mongo/documents`, `internal/adapters/mongo/repositories`, `internal/discord/features/poll` | `polls` | interactions/buttons/selects/attachments | in-progress | BSON fixture, fake repository contract, handler UI tests, artifact/export tests, side-effect select adapter tests, config/command-sync/preflight guard tests | Poll Wave A/B implements create/vote/owner menu/max-choice/result chart/`discord.txt`/`poll_info.xlsx`; runtime enablement requires `MHCAT_FEATURE_POLLS_ENABLED=true`; command sync includes polls only with staging guild `MHCAT_COMMAND_SYNC_INCLUDE_POLLS=true`; live staging smoke still pending |
| Lottery | `lotter_create.js`, `events/btn.js` lottery components | `internal/core/services/lottery`, `internal/adapters/mongo/repositories`, `internal/discord/features/lottery` | `lotters` existing rows; no creation writes | interactions/buttons/attachments/channel sends | in-progress | domain/document/repository/service/parser/handler/app/config/preflight tests | `/抽獎設置` preserves the disabled response behind `MHCAT_FEATURE_LOTTERY_DISABLED_COMMAND_ENABLED`; existing numeric `lotter*` enter/search/reroll/stop buttons are independently implemented behind `MHCAT_FEATURE_LOTTERY_COMPONENTS_ENABLED`, with rollback-compatible participant/end writes, participant text export, winner sends, and destructive-action authorization; creation and public panel generation remain disabled |
| Announcements | `announcement.js`, `announcement_set_channel.js`, `ann_message.js` | `internal/core/services/announcements`, `internal/discord/features/announcements`, `internal/adapters/mongo/repositories` | `guilds`, `ann_all_sets` | slash interactions, modals, components, message create relay, Discord channel send/delete | in-progress | domain/service/document/repository/handler/app/command-sync/preflight tests | `公告頻道設置` implemented behind `MHCAT_FEATURE_ANNOUNCEMENT_CONFIG_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_ANNOUNCEMENT_CONFIG=true`; `公告發送` modal preview/confirm/send implemented behind `MHCAT_FEATURE_ANNOUNCEMENT_SEND_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_ANNOUNCEMENT_SEND=true`; bound message relay from `ann_message.js` implemented behind `MHCAT_FEATURE_ANNOUNCEMENT_RELAY_ENABLED=true` plus explicit gateway/Guild Messages/Message Content flags; Go uses versioned confirmation IDs and suppresses tag mentions as an intentional safety fix; relay sends before deleting the original and ignores empty-content messages |
| Logging | `LoggingSystem.js`, `create_logging.js` | `internal/core/services/moderation`, `internal/adapters/mongo/repositories`, `internal/discord/features/logging` | `loggings` | setup interaction/select; message edit/delete, channel update, and voice join/leave emitters | in-progress | command definition, service, BSON document, handler, app wiring, command-sync, preflight, event-conversion, message-event, channel-event, and voice-event tests | `/set-log-channel` and legacy-style select config are implemented behind `MHCAT_FEATURE_LOGGING_CONFIG_ENABLED=true` plus `MHCAT_COMMAND_SYNC_INCLUDE_LOGGING_CONFIG=true`; new messages use versioned custom IDs while legacy `loggin_create` orphaned components are recognized with a safe rerun error; message update/delete log embeds are implemented behind `MHCAT_FEATURE_LOGGING_MESSAGE_EVENTS_ENABLED=true`, Gateway, Guild Messages, and Message Content, using cached old/deleted message payloads and best-effort audit-log delete attribution; channel topic/permission log embeds are implemented behind `MHCAT_FEATURE_LOGGING_CHANNEL_EVENTS_ENABLED=true`, Gateway, cached old channel data, and best-effort audit-log actor lookup; voice join/leave embeds are implemented behind `MHCAT_FEATURE_LOGGING_VOICE_EVENTS_ENABLED=true`, Gateway, Voice State intent, and cached member/channel data; channel moves and mute/deafen-only updates intentionally emit nothing to match legacy |
| Moderation/warnings | `slashCommands/警告系統/warnings.js`, `warn.js`, `erros_set.js`, `remove-warn.js`, `remove-all-warnings.js`; `clear.js`, `delete_data.js` | `internal/core/services/moderation`, `internal/adapters/discordgo`, `internal/adapters/mongo/repositories`, `internal/discord/features/moderation` | `warndbs`; `errors_sets`; `join_messages`, `leave_messages`, `loggings`, `numbers`, `chats`, `verifications`, `text_xp_channels`, `voice_xp_channels`, `tickets` for delete-data | warning history/settings/removal/issue interactions; optional kick/ban threshold actions; message cleanup delete side effect; delete-data select | in-progress | definition/service/repository/handler/app/config/command-sync/preflight tests for history/settings/removal/issue/message-cleanup/delete-data | `/警告紀錄` read-only lookup is gated by `MHCAT_FEATURE_WARNINGS_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_WARNINGS=true`; `/警告設定` writes and reads `errors_sets` behind `MHCAT_FEATURE_WARNING_SETTINGS_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_WARNING_SETTINGS=true`; `/警告清除` and `/警告全部清除` mutate only `warndbs` behind `MHCAT_FEATURE_WARNING_REMOVAL_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_WARNING_REMOVAL=true`; `/警告` appends `warndbs`, sends legacy-style DMs, preserves role hierarchy checks, and can kick/ban on configured thresholds behind `MHCAT_FEATURE_WARNING_ISSUE_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_WARNING_ISSUE=true`; `/刪除訊息` deletes recent Discord messages only behind `MHCAT_FEATURE_MESSAGE_CLEANUP_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_MESSAGE_CLEANUP=true`, with no Mongo writes; `/刪除資料` deletes the selected guild-scoped legacy config target behind `MHCAT_FEATURE_DELETE_DATA_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_DELETE_DATA=true` |
| Reaction/button roles | `role.js`, `role_delete.js`, `releadd.js`, `message_reaction.js`, `btn.js` | `internal/core/services/roles`, `internal/discord/features/roles` | `message_reactions`, `btns` | reactions/buttons/roles | in-progress | domain/service/repository/handler/app/config/command-sync/preflight tests | slash setup/delete, legacy button modal/panel, button add/remove, and reaction add/remove role paths are implemented behind `MHCAT_FEATURE_ROLE_SELECTION_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_ROLE_SELECTION=true`; requires gateway and Guild Message Reactions intent; production partial-fetch behavior still needs live staging validation |
| Anti-scam/safe server | `safe_server.js`, `not_a_goodweb.js`, `report_web.js` | `internal/core/services/safety`, `internal/adapters/mongo/repositories`, `internal/discord/features/safety` | `good_webs`, `not_a_good_webs` | config interaction, report webhook, and message delete implemented | in-progress | domain/service/document/repository/handler/app/config/command-sync/preflight tests for config toggle, report command, and message deletion; URL validation tests; webhook adapter tests | `/防詐騙網址` toggles legacy-compatible `good_webs.open` behind `MHCAT_FEATURE_ANTI_SCAM_CONFIG_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_ANTI_SCAM_CONFIG=true`; `/詐騙網址回報` reads `not_a_good_webs.web`, sends the legacy report webhook behind `MHCAT_FEATURE_ANTI_SCAM_REPORT_ENABLED=true`, `MHCAT_REPORT_WEBHOOK_URL` or `REPORT_WEBHOOK`, and `MHCAT_COMMAND_SYNC_INCLUDE_ANTI_SCAM_REPORT=true`; Go updates duplicate `{guild}` rows instead of legacy delete+insert as a data-safety fix, escapes the report regex, sanitizes webhook code blocks, does not write to `not_a_good_webs`, and deletes matching scam messages with the legacy warning behind `MHCAT_FEATURE_ANTI_SCAM_MESSAGE_DELETE_ENABLED=true` plus Gateway, Guild Messages, and Message Content; `create_hours` account-age gate is tracked under onboarding/account-age |
| Auto notifications/cron | cron commands, `handler/cron.js`, `handler/gift.js` | `internal/core/services/notifications`, `internal/adapters/mongo/repositories`, `internal/discord/features/notifications`, `internal/jobs`, `cmd/mhcat-economy-reset`, `cmd/mhcat-work-payout`, `internal/adapters/mongo.SchedulerLeaseStore` | `cron_sets`, `coins`, `work_*`, `gift_changes`, `mhcat_scheduler_locks` | setup/list/delete interactions; lease-backed scheduled sends | in-progress | config/delivery domain, service, document, repository, handler, wizard, worker, app, command-sync, preflight, lease, race, and fake-time tests; daily-reset and work-payout dry-run/apply/worker tests | Config commands are implemented behind `MHCAT_FEATURE_AUTO_NOTIFICATION_CONFIG_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_AUTO_NOTIFICATION_CONFIG=true`; setup writes rollback-compatible payloads and previews, list filters/cleans pending drafts, and delete removes one `{guild,id}` row. Recurring delivery is separately implemented behind `MHCAT_FEATURE_AUTO_NOTIFICATION_DELIVERY_ENABLED=true`, Gateway, and scheduler lease gates. One `auto-notification-delivery` owner reconciles active rows, runs five-field cron at fixed UTC+8 named `Asia/Taipei`, reloads rows before sends, and releases ownership on shutdown; Node `handler/cron.js` must be disabled. Economy daily reset one-shot apply and the recurring midnight worker share `daily-reset`; recurring work payout shares its configured lease between CLI apply and the every-minute worker |
| Birthday | `birthday.js`, commented scheduler | `internal/core/services/birthday`, `internal/adapters/mongo/repositories`, `internal/discord/features/birthday` | `birthdays`, `birthday_sets` | config/profile/date-add interactions implemented; scheduler/roles pending | in-progress | command definition, domain/service/document/repository/handler/app/command-sync/preflight tests | `/生日系統 祝福語設定` writes rollback-compatible `birthday_sets`; `/生日系統 增加` preserves the legacy hour/minute select UI with versioned Go custom IDs and writes `birthdays` date/time fields after completion; `/生日系統 是否允許管理員設定`, `/生日系統 刪除`, and `/生日系統 生日列表` read/write legacy-compatible `birthdays` rows behind `MHCAT_FEATURE_BIRTHDAY_CONFIG_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_BIRTHDAY_CONFIG=true`; Go intentionally persists the allow-admin setting that legacy forgot to save, suppresses management-flow mention pings, and keeps existing birthday rows unchanged when add-flow pending state expires; birthday sender/role scheduler and temporary role delivery remain disabled |
| Stats channels | stats commands, `channel_status.js` | `internal/core/services/stats`, `internal/adapters/mongo/repositories`, `internal/discord/features/stats` | `numbers`, `role_numbers` | static interaction reply, channel create, config-row delete, channel rename worker | in-progress | definition, service, repository, handler UI, app wiring, command-sync/preflight gate tests; rename service/config/app tests | `/統計系統查詢` static help is implemented behind `MHCAT_FEATURE_STATS_QUERY_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_STATS_QUERY=true`; `/統計系統創建` creates the legacy stats category/base channels, can add channel-count/text-count/voice-count stat channels, and writes `numbers` behind `MHCAT_FEATURE_STATS_CREATE_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_STATS_CREATE=true`; `/統計身分組人數` creates the legacy role-count channel and replaces `role_numbers` rows behind `MHCAT_FEATURE_STATS_ROLE_COUNT_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_STATS_ROLE_COUNT=true`; `/統計系統刪除` deletes legacy `numbers` rows behind `MHCAT_FEATURE_STATS_DELETE_ENABLED=true` and `MHCAT_COMMAND_SYNC_INCLUDE_STATS_DELETE=true`, preserving the legacy success/error embeds and not deleting Discord channels; `channel_status` rename worker is implemented behind `MHCAT_FEATURE_STATS_RENAME_WORKER_ENABLED=true`, gateway, and Guild Members intent, scans `numbers`/`role_numbers` every 20 minutes, skips missing/API-failed channels, and must not be run beside legacy ownership for the same guilds |
| Operational tools | none legacy equivalent | `cmd/mhcat-command-sync`, `cmd/mhcat-mongo-audit`, `cmd/mhcat-mongo-index`, `cmd/mhcat-staging-preflight`, `cmd/mhcat-economy-reset`, `cmd/mhcat-work-payout`, `cmd/mhcat-scheduler-lease`, `tools/parity-audit`, future `cmd/mhcat-tools` | all | command registration/index/audit | in-progress | CLI dry-run, audit, index diff, staging preflight, daily-reset dry-run/apply/lease/worker guards, work-payout dry-run/apply/worker guards, scheduler lease CLI/tests, catalog contract tests, slash-command UI parity audit tests | command sync dry-run exists; slash-command UI parity audit reports 74 matching Go definitions with zero drift/missing names; staging preflight exists; Mongo audit and index dry-run/apply tools exist; economy daily reset has coordinated one-shot and recurring paths; work payout has coordinated one-shot and recurring paths; scheduler lease diagnostic CLI exists; full 47-model Mongo catalog exists; data repair still not-started |
